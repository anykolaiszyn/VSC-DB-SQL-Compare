# ParityLens — Findings Archive

Resolved findings moved out of `PROGRESS-LEDGER.md`'s open-findings table
per the archiving convention in
`multi-agent-idea-to-app/templates/PROGRESS-LEDGER.md`, to keep the live
table scannable. Same row shape as the live table. Findings that are
`OPEN (accepted, non-blocking; ...)` stay in the live ledger — only rows
whose status is a terminal `RESOLVED` move here.

Archived 2026-08-01 at the T-21/T-18 reconciliation checkpoint (29 rows
in the live table at time of archiving).

| ID | Severity | Source task or review | Owner | Required resolution | Status |
| --- | --- | --- | --- | --- | --- |
| M-02 | MINOR | T-01 review | N/A | `tsc -b --force` used instead of literal `tsc --noEmit`, since project-references composite builds don't support `--noEmit`; reviewer confirmed this satisfies the actual verification contract | RESOLVED (no action needed) |
| M-03 | MINOR | T-02 review | N/A | Implementation report cited the `Severity` union's six values to `DESIGN-SPEC.md` when the exact enumeration actually lives in `Idea Prompt.md` section 12; type itself is correct, only the citation is off | RESOLVED (documentation-only, no code change needed) |
| I-01 | IMPORTANT | T-03 review | N/A | Parenthesis-wrapped CTE mutation (e.g. `(WITH cte AS (SELECT 1) DELETE FROM x)`) bypassed `assertReadOnlyStatement` on all four dialects — `WITH_KEYWORD_PATTERN` didn't tolerate a leading `(`. Fixed by widening the pattern to `/^[\s(]*WITH\b/i`; re-reviewed with 20 new regression tests plus 72 fresh adversarial probes in the follow-up review, all passing | RESOLVED (confirmed by independent re-review, 2026-07-27) |
| M-05 | MINOR | T-03 review | Owner of T-17 (SQL Server connector) | SQL Server's `GO` batch separator is not recognized as a statement boundary; `SELECT 1\nGO\nDROP TABLE x` does not throw for the `sqlserver` dialect. Disclosed by implementer, independently reproduced by reviewer. Accepted as non-blocking per `DESIGN-SPEC.md`'s defense-in-depth framing (read-only credentials are the primary control); track for T-17 hardening | RESOLVED — `SqlServerConnector` rejects any `GO` batch separator (`rejectGoBatchSeparator()`) before SQL reaches `assertReadOnlyStatement`, verified live against the container by both T-17's implementer and, independently, T-17's reviewer (multiple `GO` variants: repeat-count, `GO` + `EXEC sp_rename`, lowercase/tab-indented) with server-side confirmation the target table survived unaffected each time |
| M-06 | MINOR | T-03 review | Owner of T-19 (PostgreSQL connector) | PostgreSQL dollar-quoted strings (`$$...$$`) are not stripped as literals; reviewer demonstrated a concrete bypass (`SELECT $$it's fine$$ AS x; DROP TABLE y;` does not throw — an apostrophe inside the dollar-quoted body desyncs the string-literal scanner). Accepted as non-blocking per `DESIGN-SPEC.md`'s defense-in-depth framing; track for T-19 hardening | RESOLVED — `PostgresConnector` rejects any dollar-quoted content (`rejectDollarQuoting()`, deliberately blocking all `$$...$$`/tagged `$tag$...$tag$` forms, not just the narrowest fix) before SQL reaches `assertReadOnlyStatement`, verified live against the container by both T-19's implementer and, independently, T-19's reviewer (the exact T-03-demonstrated bypass plus a tagged-delimiter-with-apostrophe variant and a variant with mutating-looking text embedded inside the quoted body) with server-side confirmation the target table survived unaffected each time |
| M-07 | MINOR | T-05 review | N/A | `compareCanonicalTypes` treats Timestamp-vs-Timestamp (and Time-vs-Time) as `Review` rather than `Compatible`. T-06 resolved this at the schema-diff layer: identical native-type strings on both sides short-circuit to no finding before `compareCanonicalTypes` is called; only genuinely differing native strings (e.g. DATETIME vs TIMESTAMP_NTZ) fall through to the Review classification. Reviewer traced the fix in code and specifically probed the edge case of two different native strings mapping to the same non-Timestamp category (INT vs INTEGER), confirming no regression | RESOLVED (confirmed by independent review, 2026-07-27) |
| T-15-01 | MINOR | T-15 review | N/A | `IMPLEMENTATION-REPORT.md` cited a malformed, nonexistent commit hash (41 hex characters, not a valid SHA-1, absent from `git log --all`) instead of the real tip commit; cosmetic documentation error only, no code impact | RESOLVED (documentation-only, no code change needed) |
| I-02 | IMPORTANT | T-07 review | N/A | `profileColumn`'s numeric metrics omitted median/stddev, citing a fabricated "task brief" quote — the brief's Interfaces table explicitly requires `min/max/mean/median/stddev`. Fixed by adding `MEDIAN(col)`/`STDDEV_SAMP(col)` to the DuckDB aggregate query; re-reviewed with independently redone hand-computation of the expected values from raw fixture data, exact match | RESOLVED (confirmed by independent re-review, 2026-07-27) |
| R-01 | MINOR | T-08 review | N/A | `parseDefinition`'s credential detection blocklist was missing `auth`, `pass`, `db_pass`, `key`, `passphrase`. Fixed by T-08a (bounded follow-up, additive-only change to the existing blocklist, mechanism unchanged); reviewer confirmed all five now blocked and detection mechanism byte-for-byte unchanged | RESOLVED (confirmed by independent review, 2026-07-28) |
| R-02 | MINOR | T-08 review | N/A | `TASK-BRIEF.md`'s cross-reference to "`Idea Prompt.md` section 6 (composite keys)" doesn't match — section 6 is actually "VS Code User Experience". Implementer flagged this honestly rather than silently working around it; the resulting `keys: string[]` design correctly supports composite keys regardless of the citation error | RESOLVED (documentation-only, no code change needed) |
| T-14-01 | IMPORTANT | T-14 review (round 1) | N/A | The brief-mandated `ORDER_ID = 1008924` worked-example test for `ORDER_AMOUNT` used identical parsed JS numbers (`125.37` both sides), trivially `===`-equal, rather than the idea doc's literal decimal-string forms (`"125.3700"` vs `"125.37"`); reviewer proved the literal string forms produced a false difference (numeric-string coercion was missing). Fixed with a local `coerceNumericString` helper inside `row-level.ts` (T-14's own file, not T-12's `normalization.ts`), applied only when numeric tolerance is configured for that column; round-2 reviewer independently reproduced the doc's literal string forms and confirmed correct matching, confirmed no false-positive coercion on non-numeric/no-tolerance-configured values, and confirmed `CUSTOMER_NAME`'s exact-difference case unaffected | RESOLVED (confirmed by independent round-2 review, 2026-07-31) |
| T-14-02 | MINOR | T-14 review (round 1) | N/A | `RowCompareOptions.numericTolerance` duplicated `NormalizationRule.numericTolerance` (already available via the `rules` parameter) instead of reading it directly — a double-configuration trap where a caller using only the documented `rules[col].numericTolerance` mechanism got silently stricter-than-intended results. Folded into the same follow-up fix as T-14-01: `compareRows` now falls back to `rules[targetColumnName]?.numericTolerance` when `options.numericTolerance` is absent for that column; round-2 reviewer independently confirmed the fallback works and precedence (options wins when both configured) is correct | RESOLVED (confirmed by independent round-2 review, 2026-07-31) |
| T-20-01 | CRITICAL | T-20 review (round 1) | N/A | `compareByHash` fetches and hashes rows via `applyNormalization` only, never applying `numericTolerance` before hashing — so it disagreed with T-14's `compareRows` on the doc's own canonical `"125.3700"` vs `"125.37"` example. Fixed in round 2 by canonicalizing any column with `numericTolerance` configured (coerce numeric-looking strings to numbers, then bucket: round-to-nearest-step for `{absolute}`, round-to-significant-figures for `{percentage}`) before it enters the hashed tuple. Round-2 reviewer independently reproduced the original failure, confirmed the fix closes it, confirmed the implementer's disclosed false-disagreement-near-bucket-edge risk (real but narrow, does not resurrect false agreement) is accurate, and searched for but found no false-agreement counterexample | RESOLVED (confirmed by independent round-2 review, 2026-08-01) |
