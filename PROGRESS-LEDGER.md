# ParityLens — Progress Ledger

## Current lifecycle state

- **Phase:** RELEASE (prompt 07) — in progress. Steps 1-2 complete. Step 3:
  security review (T-23) done; dependency/license inventory complete
  (~100 runtime deps, all permissive, no copyleft) except own-code license
  metadata was found missing entirely, routed to bounded task T-24. Steps
  4-8 not yet started.
- **Exactly one task may be active:** T-24 (license metadata)
- **Last updated:** 2026-08-01, Claude Code (Lead Orchestrator)
- **Current decision maker:** alex.nykolaiszyn@gmail.com

## Task register

| Task ID | Objective | Dependencies | Agent or tool | Files owned | Status | Review state | Latest verification |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T-01 | Scaffold npm workspaces monorepo, TypeScript strict config, ESLint, Vitest, `npm run verify` | NONE | Claude Code Implementer subagent | `package.json`, `tsconfig*.json`, `eslint.config.mjs`, `vitest.config.*`, `packages/*/package.json`, `packages/*/tsconfig.json`, `packages/*/src/index.ts` | COMPLETE | APPROVED | `npm run verify` exit 0, 2026-07-27, reviewed independently by a separate subagent |
| T-02 | Define canonical shared types (`DataPlatformConnector`, `ComparisonResult`, etc.) | T-01 | Claude Code Implementer subagent | `packages/shared/src/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 2026-07-27, reviewed independently by a separate subagent; re-verified post-merge on `main` |
| T-03 | Statement-safety parser (reject mutating SQL) | T-02 | Claude Code Implementer subagent | `packages/engine/src/connector-sdk/safety/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 120/120 tests, 2026-07-27; approved after I-01 fix and re-review with 72 adversarial probes; re-verified post-merge on `main` |
| T-04 | DuckDB-backed Fixture connector + seed fixture datasets | T-02, T-03 | Claude Code Implementer subagent | `packages/engine/src/connector-sdk/fixture/**`, `packages/engine/fixtures/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 160/160 tests, 2026-07-27, reviewed independently by a separate subagent (0 findings); re-verified post-merge on `main` |
| T-05 | Canonical type-mapping layer | T-03, T-04 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/type-mapping/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 229/229 tests, 2026-07-27, reviewed independently (0 Critical/Important, 2 Minor); re-verified post-merge on `main` |
| T-06 | Schema diff | T-05 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/schema-diff/**`, `packages/shared/src/result.ts` (SchemaDifference only) | COMPLETE | APPROVED | `npm run verify` exit 0, 240/240 tests, 2026-07-27, reviewed independently (0 findings); re-verified post-merge on `main` |
| T-07 | Column profiling | T-05 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/profiling/**`, `packages/shared/src/result.ts` (ProfileDifference only) | COMPLETE | APPROVED | `npm run verify` exit 0, 249/249 tests, 2026-07-27; approved after I-02 fix and re-review with independently redone arithmetic; re-verified post-merge on `main` |
| T-08 | Parity YAML definition schema/parser | T-02, T-04 | Claude Code Implementer subagent | `packages/engine/src/orchestration/definition/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 274/274 tests, 2026-07-27, reviewed independently (0 Critical/Important, 2 Minor); re-verified post-merge on `main` |
| T-08a | R-01 hardening: extend credential-field blocklist (auth, pass, db_pass, key, passphrase) | T-08 | Claude Code Implementer subagent | `packages/engine/src/orchestration/definition/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 279/279 tests, 2026-07-28, reviewed independently (0 findings, R-01 resolved) |
| T-09 | Orchestration planner (Phase 1: schema + profile) | T-06, T-07, T-08, T-08a | Claude Code Implementer subagent | `packages/engine/src/orchestration/planner/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 283/283 tests, 2026-07-28; approved after a report-writing-only follow-up closed a Critical finding (missing IMPLEMENTATION-REPORT.md); re-verified post-merge on `main` |
| T-10 | Extension scaffold (activation, tree view, SecretStorage) | T-01 | Claude Code Implementer subagent | `packages/extension/src/activation/**`, `packages/extension/src/views/**`, `packages/extension/src/secrets/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 294/294 tests, 2026-07-28, reviewed independently (0 Critical/Important, 1 Minor); re-verified post-merge on `main` |
| T-11 | Results webview (Phase 1 scope) | T-09, T-10 | Claude Code Implementer subagent | `packages/extension/src/webview/**`, `packages/extension/src/statusbar/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 298/298 tests, 2026-07-31, reviewed independently (0 findings, adversarial XSS probe against `escapeHtml`); re-verified post-merge on `main` |
| T-12 | Column mapping + normalization rules | T-09 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/mapping/**`, `packages/engine/src/comparison-core/normalization/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 334/334 tests, 2026-07-31, reviewed independently (0 Critical/Important, 1 Minor); re-verified post-merge on `main` |
| T-13 | Volume parity | T-12 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/volume/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 339/339 tests, 2026-07-31, reviewed independently (0 Critical/Important, 1 Minor); re-verified post-merge on `main` |
| T-14 | Row-level parity | T-12 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/row-level/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 347/347 tests, 2026-07-31; round 1 CHANGES REQUIRED (T-14-01 Important), round 2 APPROVED after fix; re-verified post-merge on `main` |
| T-15 | Orchestration planner (Phase 2: volume + row-level) | T-09, T-13, T-14 | Claude Code Implementer subagent | `packages/engine/src/orchestration/planner/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 350/350 tests, 2026-07-31, reviewed independently (0 Critical/Important, 1 cosmetic Minor); re-verified post-merge on `main` |
| T-16 | Diff viewer + export (SQL preview deferred — see decision log) | T-11, T-15 | Claude Code Implementer subagent | `packages/extension/src/webview/**`, `packages/extension/src/export/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 359/359 tests, 2026-07-31, reviewed independently (0 Critical/Important/Minor); re-verified post-merge on `main` |
| T-17 | SQL Server connector | T-03, T-05 | Claude Code Implementer subagent | `packages/engine/src/connector-sdk/sqlserver/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 372/372 tests, 2026-07-31 (13 live-container integration tests), reviewed independently (0 Critical/Important, 2 accepted Minor); re-verified post-merge on `main` inside WSL |
| T-18 | Snowflake connector | T-03, T-05 | Not yet assigned | `packages/engine/src/connector-sdk/snowflake/**` | DEFERRED (owner skipped, no Snowflake trial account) | NOT REQUESTED | NONE |
| T-19 | PostgreSQL connector | T-03, T-05 | Claude Code Implementer subagent | `packages/engine/src/connector-sdk/postgres/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 386/386 tests, 2026-07-31 (14 live-container integration tests), reviewed independently (0 Critical/Important/Minor); re-verified post-merge on `main` inside WSL with both SQL Server and PostgreSQL containers live simultaneously |
| T-20 | Hash-based comparison strategy | T-14 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/hash-comparison/**` | COMPLETE | APPROVED (round 2) | `npm run verify` exit 0, 381/381 tests (408 total, 27 pre-existing live-DB-container skips), 2026-08-01; round 1 CHANGES REQUIRED on T-20-01 (Critical), round 2 fixed and independently re-reviewed/approved; re-verified post-merge on `main` |
| T-21 | Sampling strategies | T-07 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/sampling/**` | COMPLETE | APPROVED | `npm run verify` exit 0, 396/396 tests (423 total, 27 pre-existing live-DB-container skips), 2026-08-01, reviewed independently (0 Critical/Important, 2 accepted Minor); re-verified post-merge on `main` |
| T-16b | SQL preview panel (deferred from T-16) | T-07, T-09, T-13, T-15, T-16 | Claude Code Implementer subagent | `packages/engine/src/comparison-core/volume/volume.ts` (extends T-13), `packages/engine/src/comparison-core/profiling/profiling.ts` (extends T-07), `packages/engine/src/orchestration/planner/planner.ts` (extends T-09/T-15), `packages/shared/src/result.ts` (`queriesUsed` field only), `packages/extension/src/webview/**` (extends T-11/T-16) | COMPLETE | APPROVED | `npm run verify` exit 0, 368/368 tests (395 total, 27 pre-existing live-DB-container skips), 2026-08-01, reviewed independently (0 Critical/Important, 2 accepted Minor); re-verified post-merge on `main` |
| T-22 | Integration remediation: export real `@paritylens/engine` API from its package entry point; wire a `paritylens.runComparison` command (`parseDefinition` → `runComparison` → `showResultsWebview`, fixture-backed registries only) | T-04, T-08, T-08a, T-09, T-10, T-11, T-15, T-16, T-16b | Claude Code Implementer subagent | `packages/engine/src/index.ts`, `packages/extension/src/activation/activate.ts` (extends T-10), `packages/extension/package.json` (`contributes.commands` only) | COMPLETE | APPROVED | `npm run verify` exit 0, 404/404 tests (431 total, 27 pre-existing live-DB-container skips), 2026-08-01, reviewed independently (0 Critical/Important, 1 accepted Minor); re-verified post-merge on `main` |
| T-23 | Release remediation: bump `vitest` from `2.1.9` to `3.2.6+` to resolve a critical `npm audit` advisory (`GHSA-5xrq-8626-4rwp`, arbitrary file read/execution via the Vitest UI server — confirmed non-exploitable in this project since the UI server is never invoked, but a real advisory against the installed version worth fixing) | NONE | Claude Code Implementer subagent | `package.json` (`devDependencies.vitest` only), `package-lock.json` | COMPLETE | APPROVED | `npm run verify` exit 0, 404/404 tests (431 total, 27 pre-existing live-DB-container skips), 2026-08-01, reviewed independently (0 findings at any severity); re-verified post-merge on `main`; `npm audit` now shows only the pre-existing, out-of-scope `brace-expansion` finding (M-01) |
| T-24 | Release remediation: no `package.json` in the repo (root or any workspace package) declares a `license` field, and no `LICENSE` file exists — a genuine gap found during the license-inventory step, since every one of ~100 transitive runtime dependencies is permissively licensed but this project's own code had no license decision on record until now. Owner selected MIT (confirmed directly when asked) | NONE | Not yet assigned | `package.json`, `packages/shared/package.json`, `packages/engine/package.json`, `packages/extension/package.json` (`license` field only in each), `LICENSE` (new file) | ACTIVE | NOT REQUESTED | NONE |

## Integration evidence summary (prompt 06, 2026-08-01)

- **Scope confirmed ready:** every task required for this integration
  boundary (T-01–T-21 excluding deferred T-18, plus T-08a/T-16b) is
  `COMPLETE`/`APPROVED` with fresh post-merge verification recorded; no
  open Critical finding anywhere in the ledger; the two open Important
  findings (T-20-02, T-20-04) were already explicitly accepted as
  non-blocking debt at T-20's own reconciliation, not new integration
  blockers.
- **Fresh full verification:** `npm run verify` re-run on `main` at the
  start of integration — 396 passed, 27 pre-existing live-DB-container
  skips (423 total), exit 0. Matches T-21/T-16b's last recorded count
  exactly, confirming no drift since the last task merged.
- **Interface reconciliation performed:**
  - `parseDefinition` → `runComparison` chain: already exercised end-to-end
    from real YAML text (not hand-built `ParityDefinition` objects) in
    `planner.test.ts`, including a combined all-four-checks-enabled case
    (T-16b's `queriesUsed` test). Confirmed sufficient, no gap found here.
  - `writeExport`'s safe-output-root containment: independently confirmed
    against `DESIGN-SPEC.md`'s "Write safety" requirement by reading the
    implementation directly (`path.relative`-based containment check,
    resolved-root-equals-target also rejected). Matches the design
    contract as written.
  - **Real gap found:** `packages/extension/src/webview/resultsWebview.test.ts`
    and `packages/extension/src/export/exporters.test.ts` both explicitly
    disclose (in their own header comments) that they test against a
    **hand-built** `ComparisonResult` literal, never a real `runComparison`
    output. Confirmed via `grep` that no file in `packages/extension/src`
    imports from `@paritylens/engine` or calls `runComparison` at all, and
    that `packages/engine/src/index.ts` still contains only T-01's original
    `export const PLACEHOLDER = true;` — so `packages/extension` cannot
    even import the engine's real API today despite declaring it as a
    dependency. No task among T-01–T-21 ever owned closing this loop;
    T-10/T-11/T-16/T-16b's briefs each honestly and correctly scoped out
    "comparison logic"/"results rendering [of a passed-in result]," but no
    task's scope was ever "wire the two together."
- **Combined-behavior probe (temporary, run and discarded — not committed):**
  built a throwaway test in `packages/extension/src` using the same deep
  relative-import pattern the existing test suites already rely on,
  calling `parseDefinition` → `runComparison` (against `FixtureConnector`
  instances backing the `sqlserver-customer` fixture pair, deliberately
  mismatched per T-04) → `renderResultsHtml` and all three exporters, in
  one continuous run. Result: `status: "failed"`, 9 `schemaDifferences`,
  13 `rowDifferences`, `summary: {"passed":0,"warnings":7,"failed":16}` —
  a real, non-trivial mismatch-detection result — rendered through
  `renderResultsHtml` and exported through `exportToCsv`/`exportToJson`/
  `exportToMarkdown` with no error and no shape mismatch. **This confirms
  the two package boundaries are shape-compatible; the only gap is that no
  code path in this repository actually connects them.** Probe file
  deleted after use, `git status` confirmed clean before continuing.
- **Disposition:** one cross-task finding routed to a new bounded task
  (T-22, per `TASK-BRIEF.md`) rather than fixed inline during integration,
  per prompt 06's own instruction ("Do not make broad unowned fixes during
  integration"). T-22 was implemented (real `@paritylens/engine` API
  export, `paritylens.runComparison` command wiring), independently
  reviewed and **APPROVED** (0 Critical/Important, 1 accepted Minor,
  T-22-01 — see Open findings), and merged into `main` with `--no-ff`;
  `npm run verify` re-run fresh post-merge: 404/404 passed, 27 pre-existing
  skips (431 total), exit 0. **Integration is now CLOSED**: every task
  required for this boundary is `COMPLETE`/`APPROVED`, no open Critical or
  Important finding remains anywhere in the ledger, and the cross-task gap
  this check exists to catch has been found, fixed, and independently
  verified — satisfying prompt 06's "Stop when" condition in full.

## Open findings

Resolved findings are archived in `FINDINGS-ARCHIVE.md` (moved there
2026-08-01 at the T-21/T-18 reconciliation checkpoint) to keep this table
scannable. Only `OPEN`/`PARTIALLY RESOLVED` rows stay here.

| ID | Severity | Source task or review | Owner | Required resolution | Status |
| --- | --- | --- | --- | --- | --- |
| M-01 | MINOR | T-01 review | alex.nykolaiszyn@gmail.com | Transitive devDependency vulnerabilities, dev-tooling only, not shipped. Originally 10 (ESLint/minimatch, Vite/esbuild chains); T-23 (2026-08-01, vitest security bump) incidentally cleared the entire Vite/esbuild chain as a side effect of its dependency-range update. Remaining: 1 high-severity `brace-expansion` finding (ESLint's transitive dependency, `GHSA-mh99-v99m-4gvg`, DoS via unbounded expansion) — non-breaking fix available via `npm audit fix`, not yet applied since it wasn't this task's target | OPEN (accepted, non-blocking; a non-breaking `npm audit fix` remains available whenever a future bounded task wants to close it) |
| M-04 | MINOR | T-02 review | Owner of T-13/T-14 | `SchemaDifference`/`ProfileDifference`/`AggregateDifference`/`RowDifference` are currently identical aliases of a thin `DifferenceItem{severity,message}` shape. `SchemaDifference` refined by T-06, `ProfileDifference` refined by T-07 (both RESOLVED for those shapes specifically). `AggregateDifference`/`RowDifference` remain deferred to T-13/T-14 | PARTIALLY RESOLVED (2 of 4 done: SchemaDifference, ProfileDifference; AggregateDifference/RowDifference still open, owned by T-13/T-14) |
| M-08 | MINOR | T-05 review | N/A | `mapNativeType` falls back to `Unknown` for a native type string with a DDL suffix, e.g. `VARCHAR(255) COLLATE SQL_Latin1_General_CP1_CI_AS`, since `nativeType` is documented as a bare type string, not a full DDL fragment. Non-blocking; no connector currently passes such a value | OPEN (accepted, non-blocking; revisit if a future connector surfaces DDL-suffixed native types) |
| T-12-01 | MINOR | T-12 review | N/A | Abbreviation-scope-boundary tests in `mapping.test.ts` use a conditional assertion (`if (match) {...}`) rather than an unconditional one; reviewer's own adversarial probe (mismatched-ordinal case) confirmed the implementation is correct regardless — this is a test-rigor/readability nit, not a defect | OPEN (accepted, non-blocking; optional follow-up to tighten the assertion) |
| T-13-01 | MINOR | T-13 review | Owner of T-15 (planner wiring) | Undocumented precedence when a `ParityChecks.rowCount.tolerance` supplies both `percentage` and `absolute` simultaneously — self-disclosed by the implementer, not blocking. Reviewer separately probed an empty-source-table edge case with percentage tolerance (div-by-zero guard on `differenceRate`) and confirmed it surfaces as `"Informational"` rather than silently passing, so not a defect either | OPEN (accepted, non-blocking; document precedence rule when T-15 wires `compareVolume` into the planner) |
| T-14-03 | MINOR | T-14 review (round 1) | N/A | The bare-`unknown[][]` `RowSet` input branch's positional column-naming convention (`resolveColumns`) is documented but has no test exercising it — every test in `row-level.test.ts` uses the `RecordBatch` form instead | OPEN (accepted, non-blocking; add coverage if/when a future caller actually passes the bare-array form) |
| R-03 | MINOR | T-08a review | N/A | T-08a's reviewer ran its own fresh adversarial pass beyond the 5 required names and found 27 further candidate credential field names (`dsn`, `bearer`, `jwt`, `oauth_token`, `session_token`, `cert`/`certificate`, `ssh_key`, `sig`, `sas_token`, `service_account_key`, `private_key_path`, and others) also bypass the blocklist, as expected for any finite exact-name list. Informational — the blocklist is inherently open-ended and this is not evidence T-08a's bounded fix was inadequate for its stated scope | OPEN (accepted, informational; candidate list for a possible future bounded follow-up, not a current blocker) |
| T-16b-01 | MINOR | T-16b review | N/A | `queriesUsed` renders as a flat ordered list in the webview with no per-check (schema/profile/volume/row-level) or per-side (source/target) grouping. Not a defect against the brief (which specifies a flat `string[]` and no grouping requirement); a real but minor usability gap a future task could address, which would require widening `queriesUsed`'s shape (out of T-16b's authorized `result.ts` edit) | OPEN (accepted, non-blocking; candidate for a future follow-up if usability feedback warrants it) |
| T-16b-02 | MINOR | T-16b review | N/A | `runProfileChecks` calls `buildProfileQueries` a second time (once for preview collection, once again inside `profileColumn`'s own internal dispatch) — intentional and disclosed, costs only string-building CPU (the builders are pure/no I/O), not an extra query. Worth being aware of if profiling very large column counts in practice | OPEN (accepted, non-blocking; revisit only if profiling shows measurable CPU cost at scale) |
| T-20-02 | IMPORTANT | T-20 review (round 1) | Owner of a future SQL-side-hash-pushdown follow-up | Every hash level, including `"table"`, fetches the entire configured row set client-side before hashing in JS — so it delivers no wire-cost/data-transfer savings over a full row-level pull, undermining Strategy C's stated large-dataset purpose (`Idea Prompt.md`'s own scale example). Disclosed in substance (the JS-side-hashing-vs-SQL-pushdown tradeoff is documented, correctly attributed to `applyNormalization` having no SQL equivalent) but the disclosure frames it as a portability concern rather than connecting it to this specific performance consequence. Does not block approval — the brief pre-authorized disclosed-tradeoff outcomes rather than mandating SQL-side pushdown | OPEN (accepted, non-blocking; tracked as debt for a future task attempting genuine per-dialect SQL-side hash pushdown) |
| T-20-03 | MINOR | T-20 review (round 1) | N/A | `compareByHash` reads the same column-name list against both source and target fetches, so it cannot be used when source/target use different column names for the same logical field (e.g. `sqlserver-customer`'s `IsActive` vs `IS_ACTIVE`) — confirmed via a probe that raised a DuckDB binder error. Not a brief violation (`HashComparisonOptions` is new/task-owned and the brief doesn't require column-name mapping); every implementer test used `snowflake-orders`, whose column names happen to match on both sides, so the gap wasn't exercised. Recommend disclosing as a known limitation in a future revision | OPEN (accepted, non-blocking; disclose as a known limitation, candidate for a future task if real-connector column-name mismatches are hit) |
| T-20-04 | IMPORTANT | T-20 review (round 2) | Owner of a future SQL-side-hash-pushdown or hashing-canonicalization follow-up | Percentage-tolerance canonicalization (round 2's fix for T-20-01) is far weaker in practice than its own disclosure implies: round-2 reviewer's systematic scan found ~97% of within-percentage-tolerance value pairs land in different significant-figure buckets (vs. ~48% for absolute tolerance), so `compareByHash` essentially never agrees with `compareRows` on typical percentage-tolerant data, not merely "weaker at edges" as the report frames it. Does not resurrect T-20-01 (fails conservatively — no false agreement found) and does not block approval, since absolute-tolerance hashing (the doc's own worked example) is genuinely fixed | OPEN (accepted, non-blocking; disclosure should be corrected to state plainly that percentage-tolerance hashing rarely agrees with row-level comparison; tracked as debt alongside T-20-02) |
| T-21-01 | MINOR | T-21 review | N/A | The "random" strategy's `supportsTableSampling: true` branch generates `TABLESAMPLE SYSTEM (100 PERCENT)` combined with an outer `LIMIT sampleSize` — the `TABLESAMPLE` clause itself samples 100% of rows (i.e. performs no real reduction), so all actual size reduction comes from the outer `LIMIT`, not from native table sampling. Disclosed as a deliberate choice (an unknowable table size makes a smaller percentage unreliable without an extra row-count query, which would require calling `executeQuery`, explicitly prohibited for this task) but the practical effect is that this branch provides no genuine sampling-efficiency benefit over the `supportsTableSampling: false` fallback | OPEN (accepted, non-blocking; candidate for a future task if a caller supplies an estimated/known table size, enabling a genuine sampling percentage) |
| T-21-02 | MINOR | T-21 review | N/A | The `supportsTableSampling: false` fallback branch (`ORDER BY RANDOM() LIMIT sampleSize`) has no fixture test coverage, since every connector currently in the repo (`FixtureConnector`, `SqlServerConnector`, `PostgresConnector`) reports `supportsTableSampling: true` | OPEN (accepted, non-blocking; add coverage if/when a future connector reports `false`, or via a stubbed connector) |
| X-01 | MINOR | T-10 review | Owner of T-16 (or first packaging task) | Extension tests use a hand-mocked `vscode` module (Vitest) rather than `@vscode/test-electron`, so no test proves the tree view registers against a real extension-host runtime — only that it satisfies the mock's shape. Reviewer independently narrowed the actual gap: `tsc -b --force` type-checks `activate.ts`/`parityTreeDataProvider.ts` against the real installed `@types/vscode@1.85.0` package, and the hand-mocked shapes were confirmed faithful to the real `.d.ts` for the narrow surface used — so the remaining unverified risk is specifically extension-host module resolution and activation-event dispatch, not API-contract drift. Labeled X-01 (not M-01/R-01) to avoid collision with T-01's existing M-01 | OPEN (accepted, non-blocking; tracked for a real extension-host smoke test alongside T-16 or the first packaging task) |
| T-22-01 | MINOR | T-22 review | N/A | `buildFixtureRegistry` in `activate.ts` unconditionally registers a `FixtureConnector` under whatever connection-name strings a parsed `.paritylens` definition actually contains, so `UnresolvedConnectionError` can never be thrown by `paritylens.runComparison`'s own registry construction — the brief's reviewer-note scenario ("a YAML definition referencing an unregistered connection name") is structurally unreachable through this command as designed. Confirmed via reviewer's own probe: an arbitrary/unregistered-sounding connection name still produces a successful result. Not a functional defect — the generic `catch` block still handles this (unreachable) case correctly and completely — just a framing correction: the reachable error path is "any error `runComparison` throws" (e.g. an unknown fixture table, which both the implementer's own test and the reviewer's probe do exercise), not literally an unresolved connection name | OPEN (accepted, non-blocking; correct future framing/documentation of this command's error paths if revisited; only actionable if a future task wants genuine unresolved-connection-name coverage, which would require `buildFixtureRegistry` to register a fixed, smaller set of known names instead of every parsed name) |

## Blockers and dependencies

| Item | Blocking effect | Owner | Needed action | Date recorded |
| --- | --- | --- | --- | --- |
| ~~No real SQL Server/Snowflake/PostgreSQL instances or sample data available~~ RESOLVED for SQL Server and PostgreSQL (see below); Snowflake still needs a trial account or documented skip-if-unavailable path (no local-container equivalent exists) | T-17/T-19 are now unblockable; T-18 still needs its own resolution before starting | alex.nykolaiszyn@gmail.com | T-18 (Snowflake): identify a trial account, or accept documented skip-if-unavailable per its plan row | 2026-07-27, resolved 2026-07-31 (SQL Server/PostgreSQL only) |
| No agent/tool assigned yet to T-17, T-18, or T-19 | T-17/T-19 no longer have an environment blocker (see above); T-18 still needs a Snowflake target decision | alex.nykolaiszyn@gmail.com | Activate T-17 or T-19 directly; resolve T-18's Snowflake target before activating it | 2026-07-31 |
| **New:** WSL2 (native Docker Engine in Ubuntu, not Docker Desktop) does not forward container ports to Windows even with `networkingMode=mirrored` in `.wslconfig` — confirmed via direct investigation: Docker's DNAT rule is installed correctly (`iptables -t nat -L DOCKER` shows it) but no process actually binds the published port in WSL's host network namespace, so traffic never reaches the docker0 bridge from outside WSL. Windows-side `Test-NetConnection` against `localhost:14330`/`localhost:54320` and against WSL's mirrored LAN IP both fail; `curl`/Node's `net.connect` from *inside* WSL against `127.0.0.1` succeed. Separately, the WSL2 VM tears down (killing all running containers) when idle between separate `wsl.exe` invocations from Windows/PowerShell, even with containers configured `restart: unless-stopped`-equivalent behavior — each new `wsl` call from Windows starts a fresh VM boot | Any workflow needing WSL-hosted background services (DB containers) reachable from Windows-side tooling, or needing containers to persist across separate `wsl.exe` invocations | alex.nykolaiszyn@gmail.com (environment-specific, not a code defect) | Run `npm install`/`npm run verify`/connector integration tests **from inside WSL** (Node v24.9.0 installed via `nvm` at `~/.nvm`, confirmed working: 359/359 tests pass against the same repo via `/mnt/v/...`), and bring `docker compose -f docker-compose.test.yml up -d` up in the **same continuous WSL invocation** as the test run (confirmed: containers healthy + Node `net.connect` succeeds on `127.0.0.1:14330`/`127.0.0.1:54320` within one session) — do not assume containers survive between separate `wsl.exe` calls | 2026-07-31 |
| Finding-ID series collision: T-10's reviewer independently labeled its finding "M-01," already taken by T-01's review | Reviewer subagents have no visibility into prior finding IDs when self-labeling, so a repeat is inevitable without a rule | alex.nykolaiszyn@gmail.com | Orchestrator renamed to X-01 in this ledger; kit patch needed so future reviewer dispatches either check existing IDs first or the orchestrator renumbers before reconciling, consistently | 2026-07-28 |

## Decisions and approvals

| Date | Decision | Rationale | Approver | Affected tasks |
| --- | --- | --- | --- | --- |
| 2026-07-27 | Adopted the `multi-agent-idea-to-app` lifecycle kit for this project; seeded `AGENTS.md`, `PROJECT-BRIEF.md`, and this ledger from `Idea Prompt.md` | User requested the idea be run through the kit's file-backed lifecycle, working one prompt at a time | Project owner | Sets up Discovery as the next step |
| 2026-07-27 | Completion boundary set to full MVP (all connectors and check layers from the idea document) | Owner chose full scope over a smaller Phase 1 slice | Project owner | All future task planning must sequence full-MVP scope into reviewable slices |
| 2026-07-27 | Development/validation will target local or mocked fixtures (e.g. DuckDB/SQLite) instead of real database instances | No real SQL Server/Snowflake/PostgreSQL instances or sample data are available yet | Project owner | Design must define a local fixture strategy; real-connector validation deferred |
| 2026-07-27 | Project owner recorded as decision maker/approver for all lifecycle gates | No other approver identified; single-owner project | Project owner | Applies to all future approval gates |
| 2026-07-27 | `PROJECT-BRIEF.md` approved | Owner reviewed and approved the drafted brief with no changes requested | Project owner | Unblocked Design phase |
| 2026-07-27 | Architecture: TypeScript VS Code extension with in-process TypeScript comparison engine backed by DuckDB | Recommended option in Design; single-language codebase, simpler packaging, matches idea document's own recommended direction | Project owner | All implementation tasks in the Comparison Core and Orchestration API |
| 2026-07-27 | Connectors: official per-platform Node drivers (mssql, snowflake-sdk, pg), each implementing a shared `DataPlatformConnector` interface | Recommended option; native support, no extra runtime dependency vs. ODBC | Project owner | Connector SDK tasks |
| 2026-07-27 | Development/testing target: DuckDB-backed fixture connector implementing the same `DataPlatformConnector` interface as real connectors | Recommended option; unblocks Design/implementation without real database access, no rework needed when real connectors are validated later | Project owner | Connector SDK and integration/validation tasks |
| 2026-07-27 | Query safety: hard-block statement parsing (rejects mutating SQL) in addition to read-only credentials; conservative user-configurable default row cap (100,000) and timeout (60s) | Recommended option; defense in depth against user error with a writable credential | Project owner | Connector SDK safety-parsing tasks |
| 2026-07-27 | `DESIGN-SPEC.md` approved section by section (scope, architecture, data flow, security, testing/release, acceptance criteria) | Owner confirmed the recommended option at each material decision point | Project owner | Unblocked Implementation Planning phase |
| 2026-07-27 | Repo tooling: Vitest + ESLint + TypeScript strict, npm workspaces monorepo (`packages/shared`, `packages/engine`, `packages/extension`) | Recommended option; enforces the module boundary from `DESIGN-SPEC.md` at the package level | Project owner | T-01 and all subsequent tasks' verification commands |
| 2026-07-27 | `IMPLEMENTATION-PLAN.md` approved: 21 tasks (T-01 through T-21) across Foundation, Phase 1 (schema/profile), Phase 2 (keyed/row-level), and Phase 3 (real connectors, hash, sampling) | Owner approved the full dependency-ordered plan with no changes requested | Project owner | Unblocks starting T-01; sets T-01 as next active task |
| 2026-07-27 | T-01 implemented by a Claude Code Implementer subagent, independently reviewed by a separate Claude Code Reviewer subagent, approved with 0 Critical, 0 Important, 2 Minor findings (transitive dev-dependency audit warnings; `tsc -b --force` used in place of `tsc --noEmit` for project-references composite builds) | Reviewer independently re-ran `npm install`/`npm run verify`/`npm audit` and confirmed the implementer's claimed evidence byte-for-byte; scope stayed within TASK-BRIEF.md's owned files | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled | T-01 complete; unblocks T-02 and T-10 |
| 2026-07-27 | Established `main` as the trunk by renaming `task/T-01-scaffold` (the repo's only commit) rather than merging into a separate `main` | Repo had no prior commits and no divergent history to merge; renaming is equivalent and simpler | Project owner (confirmed via question) | Sets the branch policy baseline for all future tasks in `IMPLEMENTATION-PLAN.md` |
| 2026-07-27 | T-02 implemented by a Claude Code Implementer subagent, independently reviewed by a separate Claude Code Reviewer subagent, approved with 0 Critical, 0 Important, 2 Minor findings (M-03 citation correction, M-04 deferred difference-shape refinement); branch merged into `main` with `--no-ff` and re-verified green post-merge | Reviewer independently confirmed field-for-field fidelity to `Idea Prompt.md` sections 2, 9, and 11, and re-ran all tests/checks itself | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-02 complete; unblocks T-03, T-04, T-05 (indirectly), T-08, and T-09 |
| 2026-07-27 | T-03 first review round returned CHANGES REQUIRED on finding I-01 (Important: paren-wrapped CTE mutation bypass); a bounded regression task fixed it (widened `WITH_KEYWORD_PATTERN`) with new red-state evidence, and a fresh independent re-review with 72 adversarial probe cases confirmed the fix and found nothing new. Approved and merged into `main` with `--no-ff` | Demonstrates the kit's review-gate working as intended: an Important finding blocked completion until independently re-verified, not just self-reported as fixed | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-03 complete; unblocks T-05 jointly with T-04 |
| 2026-07-27 | Two Minor findings from T-03 review (M-05: SQL Server `GO` separator not recognized; M-06: PostgreSQL dollar-quoting can desync the scanner) accepted as non-blocking residual risk under `DESIGN-SPEC.md`'s defense-in-depth framing (read-only credentials are the primary control, this parser is a secondary layer) | Both are narrow, single-dialect edge cases disclosed honestly by the implementer and independently confirmed by the reviewer; fixing them now would be scope creep onto T-17/T-19's territory | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator concurred | Tracked as follow-up hardening for T-17 (M-05) and T-19 (M-06) |
| 2026-07-27 | T-04 implemented (FixtureConnector via `@duckdb/node-api`, wired to T-03's safety parser, 3 seed fixture pairs with deliberate schema/volume/row-level mismatches for SQL Server/Snowflake/PostgreSQL shapes), independently reviewed and approved with 0 findings at any severity; branch merged into `main` with `--no-ff`, dependencies re-installed and re-verified green post-merge | Reviewer independently verified all 9 `DataPlatformConnector` methods, confirmed non-vacuous T-03 wiring by reading the actual `executeQuery` code path, and checked every claimed fixture mismatch by opening the fixture files directly rather than trusting the report's prose | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-04 complete; unblocks T-05 (jointly with T-03, both now satisfied) |
| 2026-07-27 | `packages/engine/tsconfig.json`'s `rootDir`/`include` change (needed for the new `fixtures/` directory to typecheck) and the `@duckdb/node-api` dependency addition, though outside T-04's literal declared file list, accepted as minimal, mechanically-necessary companions to the owned-files work rather than scope creep | Reviewer confirmed the changes were the minimum required for the task's new directory to compile, touched no other package, and didn't expand `outDir` or leak build artifacts | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator concurred | Establishes precedent: a task may touch its own package's tsconfig/package.json when strictly necessary for its owned files to build, without requiring a brief revision |
| 2026-07-27 | T-05 implemented (`mapNativeType`/`compareCanonicalTypes` reproducing all 5 `Idea Prompt.md` section 2 worked examples exactly), independently reviewed and approved with 0 Critical/Important, 2 Minor findings (M-07: Timestamp/Timestamp Review over-flagging, M-08: DDL-suffix edge case); branch merged into `main` with `--no-ff` and re-verified green post-merge | Reviewer independently re-derived all 5 worked examples from scratch using its own throwaway test rather than trusting the report, and specifically probed the implementer's disclosed judgment call (same-category Timestamp downgraded to Review) for a real regression risk | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-05 complete; jointly with T-03 unblocks T-17/T-18/T-19; unblocks T-06 and T-07 |
| 2026-07-27 | T-06 implemented (`compareSchemas`, refining `SchemaDifference` from a thin alias to a real shape, resolving M-07 via an identical-native-type short-circuit), independently reviewed and approved with 0 findings; branch merged into `main` with `--no-ff` and re-verified green post-merge | Reviewer traced the M-07 fix in actual code rather than trusting tests, and specifically probed an edge case (different native strings mapping to the same non-Timestamp category) to confirm the fix was additive, not a narrow patch that broke adjacent behavior | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-06 complete; unblocks T-09 (jointly with T-07 and T-08, once those complete) |
| 2026-07-27 | T-06's edit to `packages/shared/src/types.test.ts` (outside its literal declared file list, needed to fix a test literal broken by the authorized `SchemaDifference` widening) accepted as a minimal mechanical consequence, following the T-04 precedent | Reviewer confirmed the diff only added the two newly-required fields to an existing test literal, with no change to test intent or assertions | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator concurred | Reinforces the T-04 precedent: a task may touch files outside its literal list when strictly, mechanically required by an explicitly authorized change elsewhere in the same task |
| 2026-07-27 | T-07 first review round returned CHANGES REQUIRED on I-02 (Important: numeric metrics omitted median/stddev, citing a fabricated "task brief" quote that did not exist in `TASK-BRIEF.md`); a bounded regression task fixed it (added `MEDIAN(col)`/`STDDEV_SAMP(col)` to the DuckDB aggregate query) with independently redone hand-computation confirming the fix, and a fresh independent re-review approved with 0 findings. Branch merged into `main` with `--no-ff` | The root cause was traced: the Lead Orchestrator's dispatch prompt to the T-07 implementer had paraphrased the brief's numeric-metrics requirement with looser language ("nice-to-have if time-constrained") not present in `TASK-BRIEF.md` itself, and the implementer treated the paraphrase as authoritative. The review gate caught the resulting gap against the actual written contract | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-07 complete; unblocks T-09 (jointly with T-06 and T-08, once T-08 completes) |
| 2026-07-27 | Process correction: future task dispatches must quote the authoritative `TASK-BRIEF.md` contract language verbatim for required interfaces, rather than paraphrasing it, to avoid a repeat of I-02's root cause | Paraphrasing in a dispatch prompt is not authoritative and can silently diverge from the brief; the review gate is a safety net for this, not a substitute for precise dispatch instructions | Lead Orchestrator (self-correction) | Applies to all future task dispatches (T-08 onward) |
| 2026-07-27 | T-08 implemented (`parseDefinition` for the Parity YAML definition, using the `yaml` library's safe default parse, rejecting inline credentials via a field-name blocklist plus a structural bare-string rule for `connection` references), independently reviewed and approved with 0 Critical/Important, 2 Minor findings (R-01: blocklist gap, honestly disclosed by the implementer and independently confirmed real by the reviewer; R-02: documentation-only citation mismatch); branch merged into `main` with `--no-ff`, dependency installed, and re-verified green post-merge | Reviewer adversarially probed the disclosed credential-detection gap with concrete test cases rather than accepting the implementer's own characterization at face value, and judged severity against the brief's literal scope rather than an idealized standard | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-08 complete; unblocks T-09 (jointly with T-06 and T-07, now all three satisfied) |
| 2026-07-27 | Owner decision: harden T-08's credential blocklist (R-01) with a bounded follow-up task before starting T-09, rather than deferring to T-17/T-18/T-19 | T-09 is the first task to resolve named connections toward real credentials, making the gap more consequential from that point forward | Project owner (confirmed via `AskUserQuestion`) | Spawned T-08a as a bounded pre-T-09 task |
| 2026-07-28 | T-08a implemented (extended the credential-field blocklist with `auth`/`pass`/`db_pass`/`key`/`passphrase`, mechanism unchanged), independently reviewed and approved with 0 blocking findings; R-01 marked resolved; branch merged into `main` with `--no-ff` and re-verified green post-merge. Reviewer's own fresh adversarial pass against 27 further candidate names produced new informational finding R-03 | Confirmed the fix was purely additive (diff review) and the specific gap R-01 named is closed; an inherently open-ended blocklist having further theoretical gaps is expected, not evidence of an inadequate bounded fix | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-08a complete; unblocks T-09 (all dependencies — T-06, T-07, T-08, T-08a — now satisfied) |
| 2026-07-28 | Formalized Implementer/Reviewer/Orchestrator roles as reusable definitions (`~/.claude/agents/{implementer,reviewer,orchestrator}.md`), vendored copies into `multi-agent-idea-to-app/agents/`, and patched the kit itself (`HANDBOOK.md`, `TASK-BRIEF.md`/`PROGRESS-LEDGER.md`/`REVIEW-REPORT.md` templates, `03-implementation-plan.md`, `05-task-review.md`) with lessons from T-01–T-09: verbatim-citation discipline (root cause of I-02), an "accepted, non-blocking, tracked" finding-status vocabulary, mandatory reviewer adversarial probing, the lettered-sibling-task convention (T-08a's own pattern), post-merge re-verification, and task-sizing guidance. Added `CLAUDE.md` and a new `00-bootstrap-and-environment.md` prompt for environment setup on a new machine/IDE, plus session-maintenance (compaction) guidance in `orchestrator.md` | User requested token-usage optimization via dedicated subagents, then asked how the framework was holding up given real experience so far — both answered by capturing the accumulated ad hoc dispatch instructions as durable, reusable artifacts rather than re-deriving them every task | Lead Orchestrator, per user request | Applies to all task dispatches from T-10 onward; carried on the T-09 branch and merged together with T-09's implementation |
| 2026-07-28 | T-09 implemented (`runComparison`, Layer-1 connectivity short-circuit, schema+profile checks, Phase 2/3 fields proven empty), first review round found the code sound (0 code-level findings, confirmed via an adversarial spy-connector probe of the connectivity short-circuit) but raised a Critical finding that `IMPLEMENTATION-REPORT.md` was never written; a report-writing-only follow-up (no code changes) closed the gap, and a second review round independently confirmed the report's accuracy against a fresh verification run before approving. Branch (carrying T-09 plus the accumulated kit/tooling work above) merged into `main` with `--no-ff`, dependencies reinstalled, and re-verified green post-merge | Demonstrates the newly-formalized `reviewer` agent definition working as intended on its first real dispatch — catching a process gap (missing evidentiary artifact) distinct from a code defect, and requiring genuine re-verification rather than trusting the fix report | Claude Code Independent Reviewer subagent (recommendation, both rounds); Lead Orchestrator reconciled and merged | T-09 complete; unblocks T-12 outright, and jointly with T-10 (once it exists) unblocks T-11 |
| 2026-07-28 | Process correction: reached the point of dispatching T-10's implementer without having compacted after T-09's reconciliation, despite `orchestrator.md` step 8 already existing as guidance. Rewrote step 8 from advisory ("consider compacting") into a hard gate ("do not proceed to the next task without either compacting or explicitly telling the human you're deliberately deferring it and why"), synced into both `~/.claude/agents/orchestrator.md` and the vendored kit copy, and updated `CLAUDE.md`'s pointer to match | User caught the skip directly ("did you compact?") and asked for it to become a hard constraint, not just documented advice; advisory language had already proven insufficient once | Project owner, via direct correction | Applies to every task reconciliation from T-10 onward |
| 2026-07-28 | T-10 implemented (extension activation, "DATA PARITY" tree view with Connections/Comparisons/Recent Runs, `SecretStore` wrapper touching only `vscode.SecretStorage`), independently reviewed and approved with 0 Critical/Important, 1 Minor finding (X-01: mocked-`vscode` test harness leaves extension-host runtime loading unverified, though the reviewer confirmed the mock's shapes and the type-checker's contract validation are both faithful to the real `@types/vscode` package). Branch merged into `main` with `--no-ff`, dependencies reinstalled, and re-verified green post-merge | Reviewer went beyond accepting the implementer's disclosed limitation at face value — independently confirmed via `tsc -b --force` that the real `@types/vscode` shapes were contract-checked, narrowing the actual unverified gap to runtime-only concerns | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-10 complete; unblocks T-11 (jointly with T-09, now both satisfied) |
| 2026-07-28 | T-10's reviewer independently labeled its one finding "M-01," colliding with T-01's pre-existing M-01. Renumbered to X-01 in the ledger to disambiguate | Reviewer subagents self-label findings without visibility into the project's existing finding-ID history; a collision was a matter of time, not a one-off mistake | Lead Orchestrator (in-session correction) | Kit patch needed: `05-task-review.md` should instruct reviewers to check `PROGRESS-LEDGER.md`'s existing finding IDs before assigning a new one, or the finding-ID scheme should be made collision-proof by construction (e.g. task-ID-prefixed IDs) |
| 2026-07-31 | Closed two agent-consistency gaps found in T-09/T-10 at the kit level (not a task-loop task, no `TASK-BRIEF.md`): implementer must commit its own work before finishing; reviewer must write `REVIEW-REPORT.md` to disk with a file tool, not just state findings in response text; orchestrator must verify commits exist before dispatching review. Synced into `~/.claude/agents/{implementer,reviewer,orchestrator}.md` and the vendored `multi-agent-idea-to-app/agents/` copies (commit `1d7fe43`); fixed a resulting stale `orchestrator.md` step cross-reference in `CLAUDE.md` (commit `f9e501f`) | User selected "Fix now" after these two gaps were observed firsthand (T-10's implementer left work uncommitted; a T-09 round-2 reviewer reported findings as text only) rather than continuing to watch for recurrence | Project owner (via "Fix now (Recommended)") | Applies to all task dispatches from T-11 onward | 
| 2026-07-31 | Selected T-11 (Results webview, Phase-1 scope) as the next active task over T-12 and T-17/T-18/T-19 | Owner chose it directly when asked (`AskUserQuestion`) — highest user-facing value of the three unblocked options, and both its dependencies (T-09, T-10) are satisfied with no external-environment blocker (unlike T-17/T-18/T-19, which need real DB instances per the blockers table) | Project owner | T-11 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-11 row |
| 2026-07-31 | T-11 implemented (`renderResultsHtml` — pure function rendering `schemaDifferences`/`profileDifferences` as HTML tables with `escapeHtml`-based sanitization and `enableScripts: false`; `formatParitySummary`/`createParityStatusBarItem` for the status bar), independently reviewed and approved with 0 findings at any severity, including a reviewer-constructed adversarial XSS probe against every string/`unknown`-typed field; branch merged into `main` with `--no-ff` and re-verified green post-merge (298/298) | Reviewer independently confirmed by direct source read (not trusting the report) that neither `renderResultsHtml` nor `formatParitySummary` has any reachable path to `vscode.workspace`, `SecretStorage`, or a connector, then went further and adversarially probed the escaping logic itself rather than accepting the "safe by construction" claim at face value | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-11 complete; T-16 (extends T-11's webview ownership) remains blocked on T-15 |
| 2026-07-31 | T-11's implementer deliberately left `packages/extension/src/activation/activate.ts` untouched — `showResultsWebview`/`createParityStatusBarItem` are exported and tested but not yet wired to any command, since no command ID or trigger is named anywhere in `TASK-BRIEF.md` or `IMPLEMENTATION-PLAN.md`'s T-11 row, and inventing one would conflict with the brief's "no run-triggering UI" prohibition. Accepted as the correct minimal reading, not a scope gap | Reviewer independently cross-checked `IMPLEMENTATION-PLAN.md`'s actual T-11 and T-16 row text (not just the brief's paraphrase) and confirmed T-16 is the task that explicitly extends T-11's webview ownership for exactly this kind of activation/trigger integration | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator concurred | Establishes precedent: activation wiring for a presentation-layer task may be deferred to the task that owns the trigger/data-source integration, when no command is named in the brief or plan |
| 2026-07-31 | Selected T-12 (column mapping + normalization) as the next active task over T-17/T-18/T-19 | Owner confirmed directly when asked (`AskUserQuestion`) — T-12 has no environment blocker and depends only on T-09 (satisfied), while T-17/T-18/T-19 are flagged in the blockers table as needing real DB instances/containers before their integration tests can complete | Project owner | T-12 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-12 row |
| 2026-07-31 | T-12 implemented (`suggestMappings` — exact/case-insensitive/snake-camel/ordinal strategies only, abbreviation matching correctly excluded per `Idea Prompt.md` section 3's MVP boundary; `applyNormalization`/`valuesEqualWithinTolerance` — pure functions applying trim/case/whitespace/date-truncation/timezone/null-equivalents with no path to mutate inputs), independently reviewed and approved with 0 Critical/Important, 1 Minor finding (T-12-01: test-assertion-strength nit, no implementation defect); branch merged into `main` with `--no-ff` and re-verified green post-merge (334/334) | Reviewer went beyond re-running the implementer's own tests: built an adversarial mutation-purity probe against nested objects/arrays/rule fields (8/8 checks green), independently re-derived the DST-boundary timezone arithmetic from scratch rather than trusting the report's math, and constructed a mismatched-ordinal abbreviation-matching case the implementer's own suite didn't cover | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-12 complete; unblocks T-13 and T-14 |
| 2026-07-31 | T-12's implementer self-caught and fixed a real host-timezone-dependent parsing bug during implementation (an initial `Date.parse`/`new Date(string)` on offset-less timestamp strings is host-local-timezone-dependent per ECMA-262; replaced with an explicit wall-clock parser using `Date.UTC` plus `Intl.DateTimeFormat`-based DST-aware offset lookup) rather than shipping a fix that happened to pass only because the dev machine's local zone matched the test's configured timezone | Disclosed proactively in the implementation report as a judgment call, not hidden; reviewer independently re-verified the fix by confirming no bare offset-less `Date.parse` call remains on the conversion path and by re-deriving the January-vs-July DST arithmetic from scratch (EST UTC-5 vs EDT UTC-4, exactly one hour apart) | Claude Code Implementer subagent (self-caught); Claude Code Independent Reviewer subagent (independently confirmed) | Reinforces that implementer-disclosed judgment calls, when genuine correctness fixes rather than scope questions, are exactly the kind of transparency the kit's process is designed to surface and reward |
| 2026-07-31 | Selected T-13 (volume parity) as the next active task over T-14 (row-level parity) | Owner confirmed directly when asked (`AskUserQuestion`) — both were unblocked and independent (T-12 satisfied for each), and T-13 is smaller, self-contained, and matches `IMPLEMENTATION-PLAN.md`'s sequencing (T-13 before T-14) | Project owner | T-13 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-13 row |
| 2026-07-31 | T-13 implemented (`compareVolume` — row-count comparison via connector `executeQuery`, following T-07's `profileColumn` query pattern exactly rather than adding a new `DataPlatformConnector` method; refined `AggregateDifference` from a placeholder alias into a real interface, the one authorized edit to `result.ts`), independently reviewed and approved with 0 Critical/Important, 1 Minor finding (T-13-01: undocumented dual-tolerance precedence, self-disclosed, non-blocking); branch merged into `main` with `--no-ff` and re-verified green post-merge (339/339) | Reviewer independently re-derived the `Idea Prompt.md` section 2 worked-example arithmetic from scratch (12,405,128 vs 12,402,991 → -2,137 difference, -0.0172% rate, FAIL against 0.0100% tolerance) rather than trusting the implementer's test assertion, confirmed `AggregateDifference`'s refinement left `SchemaDifference`/`ProfileDifference`/`RowDifference` byte-for-byte unchanged, confirmed no scope creep into distinct/duplicate/null-key counts despite `IMPLEMENTATION-PLAN.md`'s broader T-13 row wording (the brief's narrower Prohibited Changes section is authoritative), and probed an empty-source-table div-by-zero edge case beyond the implementer's own tests | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-13 complete; unblocks T-15 jointly with T-14 (still pending) |
| 2026-07-31 | Selected T-14 (row-level parity) as the next active task | Only remaining unblocked task other than T-17/T-18/T-19 (still flagged with the real-DB-instance blocker); T-13/T-14 sequencing per `IMPLEMENTATION-PLAN.md` puts T-14 second, and both must complete before T-15 can start | Lead Orchestrator | T-14 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-14 row |
| 2026-07-31 | T-14 implemented (`compareRows` — classifies rows into all 8 categories from `Idea Prompt.md` section 2 via composite-key matching and per-side duplicate detection, comparing matched rows' mapped columns through T-12's `applyNormalization`/`valuesEqualWithinTolerance`; refined `RowDifference` from a placeholder alias into a real interface). First review round returned CHANGES REQUIRED on T-14-01 (Important: the brief-mandated `ORDER_AMOUNT` worked-example test didn't actually exercise numeric-string tolerance coercion, and the doc's literal string forms produced a false difference when the reviewer tested them directly). A bounded follow-up fix (local `coerceNumericString` helper in `row-level.ts`, T-14's own file — `normalization.ts` untouched) resolved it and also folded in Minor finding T-14-02 (tolerance-config double-surface trap). A fresh independent round-2 review confirmed both fixes genuinely closed the gaps via its own adversarial reproduction, with 0 new findings. Branch merged into `main` with `--no-ff` and re-verified green post-merge (347/347) | Round-1 reviewer didn't just accept the test's pass/fail — it read the idea doc's literal source values, noticed the test's chosen representation (identical typed numbers) didn't match the doc's actual string-decimal forms, and proved the harder case was genuinely broken before flagging it; round-2 reviewer independently re-derived the same proof-of-fix rather than trusting the follow-up's own claim, matching the kit's established re-verification standard from T-07's I-02 and T-09's report-writing gap | Claude Code Independent Reviewer subagent (recommendation, both rounds); Lead Orchestrator dispatched the bounded follow-up and reconciled after round 2's approval | T-14 complete; unblocks T-15 jointly with T-13 (now both satisfied) |
| 2026-07-31 | Selected T-15 (orchestration planner Phase 2) as the next active task | Only remaining unblocked task other than T-17/T-18/T-19 (still flagged with the real-DB-instance blocker); both T-15's prerequisites (T-13, T-14) are now satisfied, and it's the natural next step to make the volume/row-level engine work actually reachable end-to-end through `runComparison` | Lead Orchestrator | T-15 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-15 row |
| 2026-07-31 | T-15 implemented (extended `runComparison` to execute volume parity via T-13's `compareVolume` and row-level parity via T-14's `compareRows` when their respective `checks.*.enabled` flags are set, populating `rowCounts`/`aggregateDifferences`/`rowDifferences` and folding the new findings into the existing `summarizeFindings`/`deriveStatus` logic unchanged; resolved carried-forward finding T-13-01 by reading `compareVolume`'s `evaluateTolerance` directly and confirming/documenting percentage-over-absolute precedence at the call site), independently reviewed and approved with 0 Critical/Important, 1 cosmetic Minor finding (T-15-01: wrong commit hash cited in the report, no code impact); branch merged into `main` with `--no-ff` and re-verified green post-merge (350/350) | Reviewer built its own spy-connector probe (not reusing the implementer's) to independently confirm zero queries fire for a disabled check and the expected query shape fires for an enabled one, forced both `compareVolume`/`compareRows` to throw and confirmed the errors propagate uncaught (an intentional asymmetry from the Layer-1 connectivity short-circuit, judged reasonable and consistent with T-09's existing precedent of not catching schema/profile query errors either), and constructed a schema/profile-disabled-but-row-count-failing case to prove Phase-2 findings genuinely flow through the untouched summary/status derivation rather than being computed and silently ignored | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-15 complete; unblocks T-16 jointly with T-11 (now both satisfied) |
| 2026-07-31 | Selected T-16 (diff viewer + export) as the next active task over T-17/T-18/T-19 | Owner confirmed directly when asked (`AskUserQuestion`) — highest user-facing value of the unblocked options, and T-17/T-18/T-19 remain flagged with the real-DB-instance/container blocker | Project owner | T-16 activated |
| 2026-07-31 | Descoped T-16's "SQL preview panel" requirement out of this task, deferred to a future follow-up task | Investigation before writing `TASK-BRIEF.md` found no engine interface exposes generated SQL as a string — `volume.ts`, `profiling.ts`, and `planner.ts`'s `fetchAllRows` each build SQL privately and execute it immediately inline. Exposing a preview surface would require editing those three engine-owned files, outside T-16's extension-only file ownership (`packages/extension/src/webview/**`, `packages/extension/src/export/**`). Asked the owner directly rather than guessing; owner chose deferral over widening T-16's ownership or having the extension re-derive SQL independently (which would risk preview text drifting from what actually executes) | Project owner (via `AskUserQuestion`, "Defer SQL preview to a follow-up task") | `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-16 row with this reduction explicit in its Objective section; a future task (not yet numbered, tentatively T-16b) will own adding query-preview string functions to the engine layer |
| 2026-07-31 | T-16 implemented (extended `renderResultsHtml` to render `aggregateDifferences` as a "Volume Parity" section and `rowDifferences` as a "Row-Level Differences" section with a nested `columnDifferences` sub-table for `matched-key-differing-values` items, preserving T-11's pure-function/no-`vscode`-import contract; added new `packages/extension/src/export/` module — pure `exportToCsv`/`exportToJson`/`exportToMarkdown` functions plus `writeExport`, which validates a resolved write path stays contained under a configurable safe output root before writing and rejects path-traversal attempts). Implementer flagged one deviation: added `@types/node` as a devDependency of `packages/extension` (types-only, no runtime dependency) since `writeExport.ts` needs Node's `fs`/`path` types with no prior precedent in this monorepo. Independently reviewed and approved with 0 Critical/Important/Minor findings; branch merged into `main` with `--no-ff` and re-verified green post-merge (359/359) | Reviewer built its own 5-case adversarial path-traversal probe (not reusing the implementer's) covering a deep relative climb, an absolute sibling-directory path, a root-itself write, an absolute path above the OS temp dir, and a legitimate control case — all rejections fired correctly with the control case still succeeding, ruling out a reject-everything false positive; grepped the diff for both `from "vscode"` and SQL-generation keywords (`SELECT`/`buildQuery`/`generateSql`/`buildSql`) to independently confirm the purity contract and SQL-preview deferral both held with zero violations; built its own `ComparisonResult` fixture with different values than the implementer's to independently confirm CSV/JSON/Markdown outputs contain correct values in correct structural positions, not just non-empty strings; judged the `@types/node` deviation reasonable (devDependencies-only, no runtime leakage outside the legitimate export module) | Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | T-16 complete; T-17/T-18/T-19 (real connectors, still blocked on real DB instances/containers) and a future SQL-preview follow-up task remain as next options |
| 2026-07-31 | Resolved the T-17/T-19 real-database-instance blocker via local Docker containers (`docker-compose.test.yml`, new file at repo root: SQL Server 2022 on port 14330, PostgreSQL 16 on port 54320, both with healthchecks) running inside WSL2's native Docker Engine (owner had Docker in WSL, not Docker Desktop for Windows) | Owner chose local containers over cloud trial/free-tier instances when asked directly. Investigation surfaced a real environment gap along the way: WSL2 does not forward these container ports to Windows even with `networkingMode=mirrored` configured (Docker's DNAT rule installs correctly but nothing binds the published port in WSL's host network namespace — confirmed via `iptables -t nat -L DOCKER`, direct `Test-NetConnection` failures from Windows against both `localhost` and WSL's mirrored LAN IP, and successful `curl`/Node `net.connect` from *inside* WSL against `127.0.0.1`). Installed Node v24.9.0 + npm v11.6.0 via `nvm` inside WSL (matching `CLAUDE.md`'s documented known-good versions exactly) and confirmed the entire toolchain works there: `npm install` and `npm run verify` both succeed against the same repo via `/mnt/v/...`, 359/359 tests passing, matching the Windows-side baseline exactly. Also discovered the WSL2 VM (and everything running in it, including containers) tears down when idle between separate `wsl.exe` invocations from Windows — confirmed by containers disappearing between two sequential PowerShell `wsl` calls despite no explicit shutdown — so containers and any test run consuming them must be started within the same continuous WSL invocation, not brought up ahead of time and assumed to persist | Project owner (chose local containers over cloud instances via `AskUserQuestion`); Lead Orchestrator (investigated and resolved the WSL networking gap, documented the working pattern) | T-17 (SQL Server) and T-19 (PostgreSQL) are now genuinely unblockable — an implementer/reviewer dispatch for either can proceed. T-18 (Snowflake) still needs its own resolution (trial account or documented skip-if-unavailable, no local-container option exists) before it can start |
| 2026-07-31 | Selected T-17 (SQL Server connector) as the next active task over T-19 | Owner confirmed directly when asked (`AskUserQuestion`) — both are now unblocked and independent (no ownership overlap), T-17 chosen first | Project owner | T-17 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-17 row, with an explicit new "Test environment" section documenting the WSL-only container reachability constraint and the run-from-inside-WSL requirement, and carrying forward T-03's M-05 (`GO` batch-separator) finding as required scope per the plan row |
| 2026-07-31 | T-17 implemented (`SqlServerConnector` — full `DataPlatformConnector` implementation against the `mssql`/Tedious driver and a live local SQL Server 2022 container, using `INFORMATION_SCHEMA`/`sys.databases` for real catalog/schema/object/column metadata, T-05's `mapNativeType` for canonical types, T-03's `assertReadOnlyStatement` on every SQL string, plus connector-level `rejectGoBatchSeparator()` resolving the carried-forward M-05 finding). Implementer disclosed three genuine cross-platform bugs found and fixed while validating against the real server (not caught by any fixture-only development, since DuckDB doesn't share these constraints): `getCatalogs` originally excluded `master`, breaking against the test container itself; the row-cap wrapper broke on any query ending in a bare `ORDER BY` (SQL Server forbids that inside a derived table) — fixed with a documented `TOP 100 PERCENT` injection; `buildProfileQuery` produced duplicate output aliases across columns (SQL Server, unlike DuckDB, rejects duplicate aliases). Independently reviewed and approved with 0 Critical/Important findings, 2 accepted Minor (T-17-01: 3+ part cross-database object references silently drop a qualifier; T-17-02: an exotic-type fallback path has no dedicated test); branch merged into `main` with `--no-ff` and re-verified green post-merge inside WSL (372/372, including all 13 live-container integration tests genuinely executing) | Both implementer and reviewer worked around a real, non-obvious environment constraint discovered while resolving the T-17/T-19 blocker: this machine's Docker only works inside WSL2, and the test container is unreachable from Windows entirely — both dispatches correctly self-diagnosed their shell environment (`uname -a`) and routed every command through WSL rather than working around the constraint with mocks or skips. Reviewer did not accept the implementer's M-05 fix or mutating-statement rejection at face value: wrote its own throwaway adversarial test file (not reusing the implementer's), probed 6 additional mutation/bypass shapes against the live container including the historical T-03 I-01 paren-wrapped-CTE-DELETE bypass pattern, and verified server-side (via a separate query path, not the connector's own `executeQuery`) that the target table survived every attempt intact | Claude Code Implementer subagent (self-caught cross-platform bugs); Claude Code Independent Reviewer subagent (recommendation, independent live-container adversarial probing); Lead Orchestrator reconciled and merged | T-17 complete; this is the project's first connector validated against a real (non-fixture) database server. T-19 (PostgreSQL) remains unblocked and independent; T-18 (Snowflake) still needs a trial-account or skip-if-unavailable decision before it can start |
| 2026-07-31 | Selected T-19 (PostgreSQL connector) as the next active task, continuing straight from T-17's reconciliation | Owner confirmed directly when asked (`AskUserQuestion`) — same pattern as T-17, no ownership overlap, containers already running, WSL workflow already validated twice (environment setup, then T-17 itself) | Project owner | T-19 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-19 row, mirroring T-17's brief structure (Test environment section, env-var-based credential handling) and carrying forward T-03's M-06 (dollar-quoting) finding as required scope per the plan row |
| 2026-07-31 | T-19 implemented (`PostgresConnector` — full `DataPlatformConnector` implementation against the `pg` driver and a live local PostgreSQL 16 container, following `sqlServerConnector.ts`'s established structural pattern without importing from it, using `information_schema`/`pg_catalog` for real catalog/schema/column metadata, T-05's `mapNativeType` for canonical types, T-03's `assertReadOnlyStatement` on every SQL string, plus connector-level `rejectDollarQuoting()` resolving the carried-forward M-06 finding by blocking all dollar-quoted content rather than the narrowest fix, disclosed as a deliberate judgment call since `assertReadOnlyStatement` itself was off-limits to edit). Independently reviewed and approved with 0 findings at any severity; branch merged into `main` with `--no-ff` and re-verified green post-merge inside WSL (386/386, both SQL Server and PostgreSQL containers live simultaneously, 27 combined live-container integration tests genuinely executing) | Reviewer did not accept the implementer's M-06 fix or mutating-statement rejection at face value: wrote its own throwaway adversarial probe (not reusing the implementer's), reproduced the exact historical T-03 bypass plus two variants of its own construction (a tagged delimiter with an embedded apostrophe, and a dollar-quoted body containing literal mutating-looking text), verified server-side via a separate raw-driver query path that the target table survived every attempt intact, and assessed the "reject all dollar-quoting" breadth as a reasonable tradeoff given the connector's read-only comparison-query scope | Claude Code Implementer subagent; Claude Code Independent Reviewer subagent (recommendation, independent live-container adversarial probing); Lead Orchestrator reconciled and merged | T-19 complete; this is the project's second connector validated against a real (non-fixture) database server. T-18 (Snowflake) is the only remaining connector task, still needing a trial-account or documented skip-if-unavailable decision before it can start; the deferred SQL-preview follow-up task (tentatively T-16b) also remains available |
| 2026-08-01 | Selected T-16b (SQL preview panel) as the next active task over T-18 (Snowflake) | Owner confirmed directly when asked (`AskUserQuestion`) — T-18 still needs a trial-account-or-skip decision before it can even be briefed, while T-16b's scope was already fully understood from T-16's own investigation | Project owner | T-16b activated; `TASK-BRIEF.md` written fresh (not from `IMPLEMENTATION-PLAN.md`, since T-16b was not itself a planned row — it is the deferred remainder of T-16's plan row) |
| 2026-08-01 | T-16b's refactor shape chosen: export small builder functions in place inside each of the three existing owned files (`volume.ts`, `profiling.ts`, `planner.ts`) rather than centralizing SQL construction into one new shared module | Owner confirmed directly when asked (`AskUserQuestion`) — minimal diff, avoids introducing a new ownership boundary for a single feature, at the cost of the task needing explicit cross-owner authorization to extend three other tasks' files | Project owner | `TASK-BRIEF.md`'s Files owned section scopes each of the three engine-file edits as narrow/additive-only, explicitly citing this decision as each original owner's implicit authorization |
| 2026-08-01 | T-16b implemented (`buildRowCountSql` in `volume.ts`, `buildProfileQueries` in `profiling.ts`, `buildFetchAllRowsSql` in `planner.ts` — each a pure builder function the corresponding execution code path now calls directly, so previewed and executed SQL are provably the same string from the same code path; `ComparisonResult.queriesUsed` added as the sole authorized `result.ts` edit; new "Query Preview" webview section added preserving T-11/T-16's pure-function/no-`vscode`-import/escaped-output contract). Implementer self-caught and fixed a genuine risk during implementation: `profileColumn` and `buildProfileQueries` could each independently evaluate `new Date()` for Date-family "now" defaults, risking a millisecond-apart preview/execution mismatch — fixed by pinning a single `now` per profiled column pair at the `planner.ts` call site and passing the same value to both. Independently reviewed and approved with 0 Critical/Important, 2 accepted Minor findings (T-16b-01: no per-check/per-side grouping in the flat `queriesUsed` list; T-16b-02: `buildProfileQueries` called twice per column pair, disclosed CPU-only tradeoff); branch merged into `main` with `--no-ff` and re-verified green post-merge (368/368 non-skipped, 395 total, 27 pre-existing live-DB-container skips) | Reviewer did not accept the implementer's core claim at face value: traced all three execution call sites directly in source to confirm each genuinely calls its new builder rather than duplicating string-building logic, then wrote its own throwaway spy-based test (not reusing the implementer's) constructing one column of each canonical family (String, Decimal, Timestamp, Boolean) against the real fixture and diffing `profileColumn`'s actually-issued SQL against `buildProfileQueries`'s output byte-for-byte; independently verified the disclosed `now`-pinning fix by grepping every non-test call site to confirm no remaining divergence path exists; probed the new webview section with an adversarial payload beyond the implementer's own XSS test | Claude Code Implementer subagent (self-caught `now`-pinning risk); Claude Code Independent Reviewer subagent (recommendation, independent spy-based SQL-equivalence verification); Lead Orchestrator reconciled and merged | T-16b complete — the SQL preview panel gap deferred from T-16 is now closed. T-18 (Snowflake connector, still needing a trial-account-or-skip decision) is the only remaining task from the original `IMPLEMENTATION-PLAN.md` set not yet started; T-20 (hash-based comparison) and T-21 (sampling strategies) are also unblocked and not yet started |
| 2026-08-01 | Selected T-20 (hash-based comparison strategy) as the next active task over T-18/T-21 | Owner confirmed directly when asked (`AskUserQuestion`) — T-18 still needs a trial-account-or-skip decision before it can even be briefed, and T-20 was chosen over T-21 | Project owner | T-20 activated; `TASK-BRIEF.md` written from `IMPLEMENTATION-PLAN.md`'s T-20 row. Explicitly scoped `compareByHash` to a single-level comparison (matching the plan's literal Interfaces-column signature) rather than the doc's narrative auto-escalating "progressive narrowing" pipeline, and left planner/`runComparison` wiring out of scope, mirroring the precedent T-13 set (comparison-core capability lands before its planner wiring in a later task) |
| 2026-08-01 | T-20 first review round returned CHANGES REQUIRED on T-20-01 (Critical: `compareByHash` never applies `numericTolerance` before hashing, so it disagrees with T-14's `compareRows` on the doc's own canonical `"125.3700"` vs `"125.37"` example) — reviewer independently constructed the exact scenario `TASK-BRIEF.md`'s Handoff note named verbatim and confirmed hashes diverge while `compareRows` classifies the pair `"matching"`. Also raised T-20-02 (Important, accepted as tracked debt: JS-side hashing pulls every row client-side, delivering no wire-cost savings over a full row-level pull) and T-20-03 (Minor, accepted: no source/target column-name-mapping support). A bounded round-2 follow-up was dispatched to the same implementer to resolve T-20-01 only | Round-1 reviewer didn't just re-run the implementer's own tests — it independently built a fresh 2-partition fixture pair to reproduce the progressive-narrowing agreement case (generalizing beyond the implementer's single `snowflake-orders` example) and separately constructed the brief's named numeric-tolerance scenario from scratch, catching a real disagreement the implementer's own test suite never exercised (only the string-casing normalization case was tested, not the numeric-tolerance case) | Claude Code Independent Reviewer subagent (recommendation, round 1); Lead Orchestrator dispatched the bounded round-2 follow-up |
| 2026-08-01 | T-20 round 2 implemented (added `canonicalizeForHash`: numeric-looking strings with `numericTolerance` configured are coerced to numbers then bucketed — round-to-nearest-step for `{absolute}`, round-to-significant-figures for `{percentage}` — before entering the hashed tuple, closing T-20-01). Implementer self-disclosed a narrow false-disagreement-near-bucket-edge risk for absolute tolerance (verified example: 125.364 vs 125.370) while arguing false agreement is impossible by construction, and disclosed percentage-tolerance canonicalization as empirically-checked-only, not proven. Round-2 review independently reproduced the original T-20-01 scenario now passing, independently verified the disclosed boundary case and searched for (and found no) false-agreement counterexample, but found the percentage-tolerance disclosure materially understated the gap (T-20-04, Important: ~97% of within-tolerance percentage pairs land in different buckets, not just an edge-case weakness) — routed as accepted non-blocking debt since it fails conservatively and doesn't affect the doc's own absolute-tolerance worked example. Approved. Branch merged into `main` with `--no-ff` and re-verified green post-merge (381/381 non-skipped, 408 total, 27 pre-existing live-DB-container skips) | Mirrors T-14's own round-1/round-2 precedent: a Critical finding blocked completion until independently re-verified fixed, not self-reported; the round-2 reviewer went beyond confirming the fix and independently pressure-tested the implementer's own disclosed limitations, finding one was understated even though it didn't block approval | Claude Code Implementer subagent (round-2 fix); Claude Code Independent Reviewer subagent (recommendation, round 2); Lead Orchestrator reconciled and merged | T-20 complete. T-18 (Snowflake, still needing a trial-account-or-skip decision) and T-21 (sampling strategies) remain as the only unstarted tasks from the original `IMPLEMENTATION-PLAN.md` set |
| 2026-08-01 | Selected T-21 (sampling strategies) as the next active task over T-18 | Owner confirmed directly when asked (`AskUserQuestion`) — T-18 still needs a trial-account-or-skip decision before it can even be briefed; T-21 has no external environment blocker | Project owner | T-21 activated; `TASK-BRIEF.md` to be written from `IMPLEMENTATION-PLAN.md`'s T-21 row |
| 2026-08-01 | T-21's implementer session was interrupted mid-work by an API/session limit before committing or verifying anything — work was in an untracked working-tree state on `main` (`sampling.ts`/`sampling.test.ts`, substantially complete but with 2 of 15 tests failing). Rather than discard it, the orchestrator created `task/T-21-sampling` properly, committed the interrupted state as an explicit WIP checkpoint, restored `main` to clean, then dispatched a fresh implementer instance to resume from that checkpoint | Preserves real, mostly-correct work rather than re-deriving it from scratch, while keeping `main` clean and the task's branch/ownership discipline intact; the WIP commit message explicitly disclosed the two known-failing tests so the resuming implementer had an honest starting point, not a claimed-complete one | Lead Orchestrator | Establishes a new precedent: a mid-task agent interruption (session/API limit, not a design failure) is recovered by checkpointing on a proper task branch and resuming, not by discarding progress or leaving `main` dirty |
| 2026-08-01 | T-21 resumed and completed (`buildSampleQuery` covering all six named strategies from `Idea Prompt.md`'s "Strategy A" — first-n, random, deterministic-hash, stratified, date-window, key-range — as pure SQL-text generation, never executing queries or threading `ExecutionOptions`, so the row-cap/timeout safety limits remain entirely the caller's responsibility). The resumed implementer investigated the 2 known-failing injection tests from the WIP checkpoint: read `FixtureConnector.quoteIdentifier`'s actual implementation, confirmed it correctly doubles embedded `"` characters, and concluded the malicious identifier becomes a safely-quoted, inert string rather than something that should throw synchronously at generation time — fixed the test expectations (not the production code) to assert the real safety property (safe quoting + `assertReadOnlyStatement` passes + execution-time DuckDB binder error + table survives), and removed leftover dead-code imports from an earlier draft. Independently reviewed and approved with 0 Critical/Important, 2 accepted Minor findings (T-21-01: `TABLESAMPLE SYSTEM (100 PERCENT)` performs no real sampling reduction, all reduction comes from the outer `LIMIT`; T-21-02: the `supportsTableSampling: false` fallback branch has no test coverage since no connector in the repo currently reports `false`); branch merged into `main` with `--no-ff` and re-verified green post-merge (396/396 non-skipped, 423 total, 27 pre-existing live-DB-container skips) | Reviewer did not accept the implementer's injection-safety conclusion at face value: independently read `quoteIdentifier`'s implementation, constructed its own adversarial identifier (not reusing the implementer's), and separately probed 5 additional parameter paths (`keyColumn`, `orderByColumn`, `startDate`, `startKey`, plus a `QueryInput`-level `{kind:"query", sql:"DELETE FROM..."}` mutation attempt) beyond what the implementer's own tests covered — the QueryInput-level probe surfaced a real but pre-existing, out-of-ownership gap (`assertReadOnlyStatement` doesn't catch subquery-nested DML, though DuckDB's own grammar rejects the malformed query anyway) already shared by `volume.ts`/`profiling.ts`/`FixtureConnector` itself, correctly judged not a T-21 regression. Reviewer also independently re-derived the row-cap review-gate proof by reading `FixtureConnector.executeQuery`'s actual wrapping logic (`SELECT * FROM (<sql>) AS fixture_query LIMIT <maxRows>`, applied unconditionally to every executed string) rather than trusting the implementer's test alone | Claude Code Implementer subagent (resumed after session-limit interruption); Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator checkpointed the interrupted WIP, dispatched the resume, and reconciled the completed task | T-21 complete. T-18 (Snowflake, still needing a trial-account-or-skip decision) is the only remaining task from the original `IMPLEMENTATION-PLAN.md` set not yet started |
| 2026-08-01 | T-18 (Snowflake connector) deferred/skipped rather than pursued | Owner confirmed directly when asked (`AskUserQuestion`, "Skip T-18 (Recommended)") — SQL Server (T-17) and PostgreSQL (T-19) connectors are already complete/approved and validated against real containers, and no engine-layer or extension-layer task depends on Snowflake specifically (every connector implements the same `DataPlatformConnector` interface); obtaining a Snowflake trial account was the standing blocker since 2026-07-27 with no local-container equivalent available | Project owner | Every task in `IMPLEMENTATION-PLAN.md` (T-01 through T-21) is now either COMPLETE/APPROVED or DEFERRED — no task remains ACTIVE or NOT STARTED-and-unblocked. If Snowflake support is wanted later, T-18 can be reactivated by resolving its trial-account blocker and following T-17/T-19's established brief/implement/review pattern |
| 2026-08-01 | Ran `multi-agent-idea-to-app/prompts/06-integration.md` (Integration phase) against the full approved task set. Confirmed step 1's precondition (every required task COMPLETE/APPROVED, no open Critical finding) directly from the ledger rather than assuming it; re-ran `npm run verify` fresh (396/396, matches last recorded count exactly). Found one genuine cross-task gap via a temporary, discarded probe: `packages/engine/src/index.ts` still only exports T-01's placeholder, so `packages/extension` cannot import `@paritylens/engine`'s real API, and no task ever wired a real `parseDefinition` → `runComparison` → `showResultsWebview` command — every extension-layer task tested rendering only against a hand-built `ComparisonResult` literal. The probe proved the two package boundaries are shape-compatible (a real `runComparison` result rendered/exported cleanly) — the only gap is that no code path connects them | Per prompt 06's own instruction ("Do not make broad unowned fixes during integration"), routed the finding to a new bounded task (T-22) rather than patching it inline; confirmed each individual task's own scope boundary (T-10/T-11/T-16/T-16b) was honest and correctly reviewed at the time — this is a plan-level gap (no task ever owned "wire the two together"), not any task's failure | Project owner (selected "New bounded task T-22" when asked); Lead Orchestrator wrote `TASK-BRIEF.md` and activated T-22 | Ledger phase set to `INTEGRATION` with T-22 as the one active remediation task; integration is not closed until T-22 completes and is independently reviewed, since prompt 07 (Release) step 1 explicitly requires integration work to be approved before release evidence can be collected |
| 2026-08-01 | T-22 implemented (`packages/engine/src/index.ts` now re-exports `parseDefinition`/`InvalidDefinitionError`, `runComparison`/`ConnectorRegistry`/`UnresolvedConnectionError`, `FixtureConnector`, following `packages/shared/src/index.ts`'s exact re-export-only precedent; a new `paritylens.runComparison` command in `activate.ts` prompts for a `.paritylens` file via `showOpenDialog`, parses it, builds a `FixtureConnector`-backed registry from whatever connection names the definition contains, runs the comparison, and shows the results webview — fixture-only, disclosed via `showInformationMessage` on every invocation). A forced, disclosed minimal edit to `activate.test.ts`'s `vi.mock("vscode", ...)` factory (added `commands.registerCommand`/`workspace.workspaceFolders` stubs) was needed as a mechanical consequence of `activate()` now calling those APIs — no assertions changed. Independently reviewed and **APPROVED** with 0 Critical/Important, 1 accepted Minor (T-22-01: `UnresolvedConnectionError` is structurally unreachable through this command's own registry-construction logic, since it registers whatever connection names the parsed definition actually contains — a framing note, not a defect; the generic error handler still covers this path correctly). Branch merged into `main` with `--no-ff` and re-verified green post-merge (404/404 non-skipped, 431 total, 27 pre-existing live-DB-container skips) | Reviewer independently re-derived every load-bearing claim rather than trusting the implementation report: confirmed scope discipline via direct diff inspection (only the three declared owned paths received logic changes), confirmed `index.ts`'s new exports are genuinely re-export-only, verified the `activate.test.ts` mock-only disclosure by reading that file's diff directly, and wrote 5 fresh adversarial probes of its own against the exported command handler (malformed YAML, an empty document, a credential-injection attempt, confirming the fixture-only notice fires even on a failing run, and the unregistered-connection-name case that surfaced T-22-01) rather than reusing the implementer's own tests | Claude Code Implementer subagent; Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged. Note: during reconciliation, the orchestrator's own ledger-edit cleanup (moving a stray uncommitted `PROGRESS-LEDGER.md`/`REVIEW-REPORT.md` diff off the task branch, mirroring the T-20 precedent) accidentally discarded the reviewer's actual `REVIEW-REPORT.md` content via `git checkout -- REVIEW-REPORT.md` on `main`; recovered in full via `git fsck --unreachable` locating the still-referenced blob (git never garbage-collects a blob still reachable from a recent working-tree state within the same session), confirmed byte-identical to the pre-discard diff before re-committing on the correct branch | **Integration (prompt 06) is CLOSED.** Every task required for this boundary is `COMPLETE`/`APPROVED`, no open Critical/Important finding remains, and the cross-task gap this phase exists to catch was found, fixed, and independently verified. Paused here per explicit owner instruction — next lifecycle step is prompt 07 (Release), not yet started, awaiting owner check-in before proceeding |
| 2026-08-01 | Ran `multi-agent-idea-to-app/prompts/07-release.md` (Release phase) steps 1-2: confirmed every required task `APPROVED` with no open Critical finding (both open Important findings, T-20-02/T-20-04, already accepted non-blocking debt), re-ran `npm run verify` fresh (404/404, 27 skips, exit 0, matches T-22's last recorded count exactly). Step 3 (security review) ran `npm audit` and found a real critical advisory: `vitest@2.1.9` (installed) has `GHSA-5xrq-8626-4rwp` (CVSS 9.8, arbitrary file read/execution via the Vitest UI server), affecting `<3.2.6`. Investigated exploitability directly rather than trusting the severity label alone: confirmed `vitest` is a root `devDependency` only (never a dependency of `packages/extension`, the package actually shipped), `@vitest/ui` is not installed anywhere in the repo, and no script/config ever passes `--ui` — the vulnerable code path is never invoked in this project's actual usage. A non-breaking fix path exists (`vitest@3.2.6`+, staying on the `3.x` line, avoiding `npm audit fix --force`'s suggested breaking jump to `4.x`) | Per this project's established "route cross-task/cross-phase defects to a bounded task rather than fix inline" discipline (same pattern as T-22 during integration), and because a real fix was available at low risk (dev-only dependency, non-major version bump), the owner chose to fix now via a bounded task rather than merely disclose-and-accept | Project owner (selected "Fix now via a bounded task" when asked); Lead Orchestrator wrote `TASK-BRIEF.md` and activated T-23 | Ledger phase set to `RELEASE` with T-23 as the one active remediation task; release evidence-gathering (steps 4 onward) is paused until T-23 completes and is independently reviewed, since a clean `npm audit` is part of this phase's own security-review evidence requirement |
| 2026-08-01 | T-23 implemented (`vitest` bumped `2.1.9` → `3.2.7` in `package.json`'s `devDependencies`, staying on the `3.x` line per the brief; `package-lock.json` regenerated via `npm install`). No test file needed any change — same-major-version bump, behavior-identical. Independently reviewed and **APPROVED** with 0 findings at any severity: reviewer confirmed fresh `npm audit` shows the `vitest`/`@vitest/mocker`/`vite-node` advisories gone, confirmed `npm ls vitest` resolves to `3.2.7` (not `4.x`), confirmed `npm run verify` reproduces the exact pre-bump test count (404/27/431, exit 0), confirmed the diff is scoped to exactly `package.json`/`package-lock.json`/`IMPLEMENTATION-REPORT.md` with nothing under `packages/**` touched, and independently verified the implementer's disclosed incidental finding (the `esbuild` advisory also cleared, as a transitive side effect of `vite` jumping to a newer range under vitest 3.2.7's dependency requirements — confirmed via direct `package-lock.json` diff, and confirmed this didn't violate the "do not touch esbuild/vite pins" prohibition since no explicit pin for either ever existed to touch). Branch merged into `main` with `--no-ff` and re-verified green post-merge (404/404 non-skipped, 431 total, 27 pre-existing live-DB-container skips); `npm audit` now shows only the pre-existing, out-of-scope `brace-expansion` finding — M-01's ledger entry updated to reflect the new, smaller vulnerability count | Mirrors T-22's pattern of a cross-phase-discovered gap routed to a small bounded task rather than fixed inline; reviewer's independent verification of the incidental esbuild-fix disclosure is a good example of this project's "verify every claim, including favorable-sounding ones, don't just probe for problems" discipline | Claude Code Implementer subagent; Claude Code Independent Reviewer subagent (recommendation); Lead Orchestrator reconciled and merged | Release (prompt 07) step 3's security-review fix is done. Remaining step 3 work: dependency and license inventory (not yet performed). Steps 4-8 (packaged artifact build, launch/smoke test, reconciliation, `RELEASE-CHECKLIST.md`, final human approval) not yet started |
| 2026-08-01 | Completed Release (prompt 07) step 3's dependency and license inventory. Direct runtime dependencies: `@duckdb/node-api`/`@duckdb/node-bindings*` (MIT, including per-platform native binaries), `mssql` (MIT), `pg` (MIT), `yaml` (ISC). Full transitive runtime tree (`npm ls --omit=dev --all`, 102 unique packages, dominated by the Azure Identity SDK chain pulled in via `tedious`) scanned offline (no network access, per this phase's own constraint — explicitly avoided `npx license-checker` after discovering it silently attempts a package download) via a local script reading each installed package's own `package.json` `license` field directly from `node_modules`: 100% permissively licensed (MIT/BSD-3-Clause/Apache-2.0/ISC/0BSD), zero copyleft/restrictive licenses found anywhere in the dependency tree. Separately found: none of this project's own four `package.json` files (root or any workspace package) declare a `license` field, and no `LICENSE` file exists at the repo root — this project's own code has never had an explicit license decision recorded | Per this project's established "route cross-phase-discovered gaps to a bounded task" discipline (same pattern as T-22/T-23); asked the owner directly which license to use rather than assuming, since this is a genuine first-time decision with real distribution-rights consequences, not a technical judgment call | Project owner (selected "Fix now via a bounded task", then selected "MIT" when asked which license — matches the license family already used by every dependency this project pulls in) | Ledger phase remains `RELEASE` with T-24 as the one active remediation task; the dependency/license inventory itself (as distinct from the missing-metadata fix) is otherwise complete and clean — no further third-party license concern found |

## Cost notes

| Date | Activity | Time or spend | Value or trade-off | Owner |
| --- | --- | --- | --- | --- |
| 2026-07-27 | Initial project setup: seeded control files from idea document | Single session, file creation only | Gives Discovery a running start instead of a blank brief | Claude Code |
| 2026-07-27 | T-01 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~187s + ~111s | Enforced implementer/reviewer independence per `AGENTS.md`; caught nothing needing rework but validated the process | Claude Code Lead Orchestrator |
| 2026-07-27 | T-02 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~346s + ~158s | Enforced implementer/reviewer independence; reviewer caught 2 Minor citation/design-debt notes, no rework required | Claude Code Lead Orchestrator |
| 2026-07-27 | T-03 implementer + reviewer + regression-fix implementer + re-review subagents (4 dispatches) | Four subagent runs, ~375s + ~274s + ~159s + ~141s | Enforced implementer/reviewer independence on a security-critical task; the extra regression round (catching a real SQL-injection-adjacent bypass) is the reason this loop exists — cost justified by outcome | Claude Code Lead Orchestrator |
| 2026-07-27 | T-04 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~680s + ~206s (largest task so far: new dependency, 3 fixture datasets, 40 new tests) | Enforced implementer/reviewer independence; reviewer's line-by-line fixture verification is exactly the kind of check that matters since this data is load-bearing for T-06/T-14's acceptance criteria | Claude Code Lead Orchestrator |
| 2026-07-27 | T-05 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~435s + ~210s | Enforced implementer/reviewer independence; reviewer wrote its own independent test file to re-derive the worked examples rather than trusting the implementer's tests, and caught a real design gap (M-07) worth carrying into T-06 | Claude Code Lead Orchestrator |
| 2026-07-27 | T-06 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~380s + ~197s | Enforced implementer/reviewer independence; task brief explicitly carried forward M-07 as required scope, demonstrating cross-task finding tracking working end to end | Claude Code Lead Orchestrator |
| 2026-07-27 | T-07 implementer + reviewer + regression-fix implementer + re-review subagents (4 dispatches) | Four subagent runs, ~457s + ~258s + ~169s + ~121s | Enforced implementer/reviewer independence; caught a real contract violation (I-02) traced back to an orchestrator dispatch-prompt error, not just an implementer mistake — cost of the extra round justified by catching a genuine authority/paraphrase failure mode | Claude Code Lead Orchestrator |
| 2026-07-27 | T-08 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~355s + ~288s | Enforced implementer/reviewer independence; dispatch prompt applied the T-07 process correction (pointed at TASK-BRIEF.md as sole authority instead of paraphrasing); reviewer's adversarial probing of the disclosed credential-detection gap produced a concrete, actionable Minor finding rather than a vague caveat | Claude Code Lead Orchestrator |
| 2026-07-28 | T-08a implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~173s + ~215s (smallest task so far, deliberately bounded) | Enforced implementer/reviewer independence on a small hardening task; kept scope tight rather than letting a Minor finding balloon into open-ended credential-detection redesign | Claude Code Lead Orchestrator |
| 2026-07-28 | Formalizing Implementer/Reviewer/Orchestrator as durable agent definitions, plus 6 kit-template patches and 2 new kit docs (bootstrap prompt, agents/README.md) | Documentation/tooling work, not billed against a task ID | One-time cost that should reduce per-dispatch token spend going forward — a dispatch is now a short task-specific prompt against a pre-loaded role definition, instead of re-explaining the full role every time | Claude Code Lead Orchestrator, per user request |
| 2026-07-28 | T-09 implementer + reviewer (round 1) + report-writing-only follow-up implementer + reviewer (round 2) subagents (4 dispatches, first using the new formal `implementer`/`reviewer` agent definitions instead of ad hoc dispatch prompts) | Four subagent runs, ~short implementation dispatch (already committed when this session resumed) + reviewer round 1 + implementer report-fix + reviewer round 2 | First real test of the formalized agent definitions; caught a genuine process gap (missing implementation report) on the first dispatch, and the fix required a full second review round rather than being waved through — validates that the shorter dispatch prompts didn't come at the cost of review rigor | Claude Code Lead Orchestrator |
| 2026-07-28 | T-10 implementer + reviewer subagents (2 dispatches, using the formal agent definitions) | Two subagent runs, ~347s + ~173s | Enforced implementer/reviewer independence; implementer's work was uncommitted when review started (orchestrator committed it before dispatching review, per the brief's Handoff checkpoint requirement); reviewer's own type-check verification narrowed the disclosed test-harness gap rather than accepting it at face value | Claude Code Lead Orchestrator |
| 2026-07-31 | T-11 implementer + reviewer subagents (2 dispatches, first task run entirely under the closed agent-consistency-gap fixes from `1d7fe43`) | Two subagent runs, ~292s + ~197s | Enforced implementer/reviewer independence; implementer committed its own work without orchestrator intervention this time (the exact gap `1d7fe43` was meant to close); reviewer wrote and ran its own adversarial XSS probe rather than accepting "no vscode access" as self-evident from reading imports alone | Claude Code Lead Orchestrator |
| 2026-07-31 | T-12 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~434s + ~235s (largest engine task since T-04/T-07: two new subdirectories, a self-caught timezone bug fix, 36 new tests) | Enforced implementer/reviewer independence; reviewer built a standalone adversarial probe script against compiled output rather than only reading source, and independently re-derived DST arithmetic from scratch instead of trusting either the implementer's report or its own test file | Claude Code Lead Orchestrator |
| 2026-07-31 | T-13 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~377s + ~188s | Enforced implementer/reviewer independence; reviewer independently re-derived the worked-example percentage-rate arithmetic from scratch rather than trusting the implementer's assertion, and caught scope-narrowing correctly applied against the plan row's broader wording | Claude Code Lead Orchestrator |
| 2026-07-31 | T-14 implementer + reviewer (round 1) + follow-up-fix implementer + reviewer (round 2) subagents (4 dispatches) | Four subagent runs, ~376s + ~372s + ~235s + ~132s | Enforced implementer/reviewer independence on the task with the most idea-doc-literal acceptance criteria so far (8-category classification, composite keys, 4-column worked example); round-1 reviewer's insistence on re-deriving whether each column's normalization claim was actually true (not just trusting the test's pass/fail) caught a genuine gap the implementer's own test suite masked — directly justifies the review gate's cost on this task | Claude Code Lead Orchestrator |
| 2026-07-31 | T-15 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~343s + ~254s | Enforced implementer/reviewer independence on the task that finally makes T-13/T-14's engine work reachable end-to-end through `runComparison`; reviewer built an independent spy-connector probe to prove disabled checks issue zero queries, rather than accepting the implementer's own test as sufficient | Claude Code Lead Orchestrator |
| 2026-07-31 | T-16 implementer + reviewer subagents (2 dispatches) | Two subagent runs, ~514s + ~205s | Enforced implementer/reviewer independence; reviewer built its own 5-case path-traversal probe suite and an independently-constructed `ComparisonResult` fixture (distinct from the implementer's) to sample export output correctness directly, rather than trusting either the implementer's test coverage or its own first-draft probe | Claude Code Lead Orchestrator |
| 2026-07-31 | Environment setup: local Docker test containers (SQL Server 2022 + PostgreSQL 16) via WSL2's native Docker Engine, not billed against a task ID | Investigation and iterative fixing across several networking layers (Docker port-publish gap, WSL VM idle-teardown, mirrored-networking limits), plus installing Node v24.9.0/npm v11.6.0 inside WSL via nvm | One-time environment cost that permanently unblocks T-17/T-19 (and any future real-connector hardening work) rather than a per-task recurring cost; the documented working pattern (bring containers up and run tests within the same continuous WSL session) is now reusable for T-19 without repeating the investigation | Lead Orchestrator, per owner's direction to set up local containers |
| 2026-07-31 | T-17 implementer + reviewer subagents (2 dispatches, first task run against a real, non-fixture database server) | Two subagent runs, ~1358s + ~460s (longest task so far — first real driver integration, live-container test infrastructure, three self-caught cross-platform bugs) | Enforced implementer/reviewer independence on the project's highest-real-world-risk task so far (a real network-facing driver, live mutating-statement rejection); reviewer wrote its own throwaway adversarial test file probing 6 additional mutation/bypass shapes beyond the implementer's coverage, including a historical T-03 bypass pattern (I-01), with server-side verification via a separate query path — the cost is directly justified by this being the project's first connector where a rejection bug would mean an actual live database write, not a simulated one | Claude Code Lead Orchestrator |
| 2026-07-31 | T-19 implementer + reviewer subagents (2 dispatches, second task run against a real, non-fixture database server) | Two subagent runs, reviewer ~500s | Enforced implementer/reviewer independence on the project's second real-network-facing-driver task; reviewer's own probe went beyond the brief's minimum ask (reproducing the exact T-03 bypass) by constructing two further dollar-quoting variants of its own design, with server-side verification via a raw-driver query path — closes out the M-05/M-06 pair of findings carried forward from T-03's original review, both now resolved with independent live verification | Claude Code Lead Orchestrator |
| 2026-08-01 | T-16b implementer + reviewer subagents (2 dispatches, fixture-only task, no WSL/Docker environment needed) | Two subagent runs, implementer ~551s, reviewer ~239s | Enforced implementer/reviewer independence on a cross-cutting refactor spanning three previously-separately-owned engine files; reviewer's own spy-based diff test across all four canonical type families is exactly the kind of check that matters for this task's core deliverable (provable preview/execution SQL equivalence) — cost directly justified by that being the one property the whole task exists to guarantee | Claude Code Lead Orchestrator |
